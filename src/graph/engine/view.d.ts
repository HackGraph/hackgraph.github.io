/**
 * The vendored view is a classic script imported purely for its side effect: evaluating it
 * publishes `globalThis.GraphView`. It has no exports of its own, and the surface the host
 * actually uses is typed in `loadView.ts`.
 */
declare const _default: unknown;
export default _default;
